# Batch of 2026-09-16: sitemap family, policy page, digest (one push)

Started 1:36 PM EDT on "continue full". Deploy: ONE push after the full gate.

| item | state | proof |
|---|---|---|
| `/perm-queue/<month>` in the sitemap (40 pages) | done | `sitemap.test.ts` 27/27 incl. the tree-walking family test; probed red by emptying the block |
| policy-changes rebuilt (strip, ledger, collapsed rows, printed first pages) | done | `policy-changes.test.tsx` 10/10, `policyFeed.test.ts` 16/16; glue 0 with control; headings 0 findings; visible words 2,270 -> 1,254 |
| ingest: Register dates, corrections, housekeeping exclusion | done, ran against production | 23 rows rewritten, 8 columns added, 1 personnel row deleted; `test_policy_notices.py` 21 PASS |
| digest fixes, flag OFF | done (fork) | 23 + 5 tests, typecheck 0; `SignalsPanel` shows the health line |
| shared floors: RecordStrip date, FinePrint summary to 14px | done | measured at 390 / 768 / 1440 |
| social card re-rendered from the new page | done | 1200x630, 76 KB, gate 122/122 |
| mobile / tablet / desktop / dark sweep (chrome-devtools MCP) | done | no overflow at 390/768/1440; no small targets of ours; strip labels 14px at 390 |
| production build | green, 10m21s | detached; `.next/` is wiped on start, so logs live in `node_modules/.cache/qa` |
| full suite (`pnpm test:run`) | green: 391 files, 7,034 tests, 12m35s (first run bailed on a 164-char description, fixed to 136) | `node_modules/.cache/qa/suite2.log` |
| push + `npx convex deploy -y` + verify | done: `786d6b5a` pushed 3:32 PM EDT, Convex deployed, Vercel Ready 3:33 PM EDT | live: page 200, 40 month URLs in pages.xml, strip served, capture image/webp, card 73 KB, glue 0, one-click route 400 on junk via both origins |
| GSC: request /policy-changes and the top month pages | next window ~11:55 AM EDT Sep 17 | quota ~11/day |

Left out on purpose: the nine hand-rolled `<details>` migration (cosmetic).

## "Go plus" items (3:08 PM EDT)

| item | state | proof |
|---|---|---|
| every subscriber email carries the one-click stop AND the preference page (focus on its row) | done | `footer-links.test.tsx` 8/8; `prefsLink.ts`; senders pass `prefsUrl` |
| digest `List-Unsubscribe` header, via `POST /prefs/unsubscribe?token=&kind=` | done | `convex-relative-urls-rewritten.test.ts` green (the `/prefs/:path*` rewrite covers it) |
| digest says "same as last week" instead of restating a bulletin | done | compose 11/11, BulletinWeekly 6/6 |
| account mail links to Settings | already true in `EmailLayout`; the gate now asserts it | |
| reminders: routine Check 4 (Sep 22 to 29), calendar Tue Sep 22 9:30 AM ET, this ledger | done | RemoteTrigger 200; event id 83ndg2kr3ujknetjn7sv6ktkb0 |

Digest go-live: flag stays OFF. Read the Sep 22 preview; if right, `NEWSLETTER_ENABLED=1` and the first send is Tue Sep 29 9:00 AM ET.
