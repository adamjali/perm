# permtracker.app read-only audit: synthesis of fifteen surfaces

Snapshot: Sun Sep 6 2026, roughly 3:30 PM to 5:45 PM EDT. Deploys frozen since Sat Sep 5 10:13 PM EDT (one production deployment, dpl_ACti54qGJZC7A2VBgM9BG51d1foa; two later docs-only pushes were correctly Canceled by the ignore script). Monday Sep 7 is the clean baseline day.

Reading guide: "measured" means the reader ran the query, read the log, or computed the number. "Inferred" means it follows from code reading or extrapolation. A finding tagged [measured=false] or [medium confidence] rests on reading, not observation; treat it as a hypothesis to probe.

---

## 1. Corrections to the earlier pass

The prior pass (P1 to P7) was mostly right about symptoms and wrong or incomplete about mechanisms.

**P1 (no new filings since Aug 28): confirmed, and the mechanism is different from what P1 implied.** The PERM prober didn't run out of span; it stopped because of `DISCOVERY_DRY_STREAK=2`. On Aug 30 it walked day code 26240 (Aug 28) from serial 199,995, hit through 200,246, then met two empty 50-number batches (the overnight lull when only automated PWD/LCA filings advance the shared counter) and abandoned the rest of Aug 28's business day. From Aug 31 every run asked the same 100 serials (200,247 to 200,346) under five day codes; on Sep 2 the 5-day window dropped 26240 itself, so every number asked since has been a (day code, serial) pair that cannot exist. Seven runs printed "10 requests, 0 new cases recorded". DOL's counter was at 215,323 by Sep 3 (measured from a visitor lookup, G-200-26246-215323). CLAUDE.md's "the frontier self-heals" is false whenever a miss spans the window. Two additions no earlier pass had: the prober only ever asks for `G-100-` numbers and reads its frontier from `G-100-` rows, while G-200 carries 22 to 30 percent of PERM filings (measured three ways: 24.8 percent of day codes >= 26200, 22.5 percent of the disclosure corpus, 28 percent of day 26238); and the serial counter wraps at 1,000,000 with 6-digit zero padding (measured May 15 2024 and Jun 9 to 12 2026), which the prober neither pads nor wraps.

**P2 (Sunday PWD/LCA full sweep timed out): confirmed and quantified.** Python was killed by `timeout 170m` at 12:38:02 PM EDT, exactly 170 minutes after "mode: full". The PWD half completed (96,009 checked in 56 min, 21 moved, recorded COMPLETE); the LCA half reached about 203,700 of 309,557 at ~1,790 cases/min and needs ~172 min alone. Total requirement is ~229 to 236 minutes, so the Sunday full has never once had a chance. It wrote ~300,000 `last_checked_at` UPDATEs for 54 real transitions, and because it walks `ORDER BY case_number`, the same newest ~100k LCA rows are the ones never re-checked, every week. Hidden extra: `pwd_case_status` carries four duplicate indexes (and `pwd_case_events` one), so each of those writes maintained twice the index entries.

**P3 (Tests red): confirmed, dated, and worse.** The last green run was Sep 3 8:09 PM EDT (43f80e3b); the first red was run 33830801948 at 10:47 PM EDT on 930500d8, whose committer time is 10:44 PM. The prior "8:09 PM" was the last pass, not the first failure. 14 of 80 checks in `test_flag_disclosure.py` fail because the parser now emits `attorney_name` and `attorney_slug` and the fixture dicts don't. The step is misnamed "Live-recent diff contract" (it runs three scripts in one `bash -e` block). The consequence nobody stated: `pnpm test:run` is the last step, so vitest has not executed in CI for 25 consecutive pushes since Sep 3. Locally, the scoped runs that were allowed (convex 1,116, ssr 11, app tests 126, turso 280) all pass.

**P4 (I-485 ingest failed): confirmed as a datacenter block, not a moved URL.** Four 403s over 4.3 minutes from the runner; the identical header set from the laptop returns 200. It's the workflow's only run ever. The script records no failure row, the workflow has no failure hook, and the stored `max_age_days` is 75 (a stale value; code now says 45 but no run has succeeded to write it), so ingest-health stays green until Oct 19.

**P5 (health check masks the failure): confirmed for PWD/LCA, refuted for PERM.** `check_ingest_health.py:125` keys on `split()[0]` and keeps the newest row per key; this is deliberate and pinned by a test. Right now the failed row (ingest_runs #57, 12:38 PM EDT) is the newest and a local read-only run of the checker exits 1. Monday's pending run (cron 09:40 UTC) is scheduled 20 minutes before the health cron (10:00 UTC) and both drift by the same queue lag; measured 4 of 4 (one reader) and 5 of 5 (another) days, pending finished 4.6 to 13.6 minutes before health. So masking is likely but not certain (the data reader called it a race). The documented backstop, a per-pass freshness key, exists only for PERM (`perm-case-status-full`); PWD/LCA stamp one `pwd-status`/`lca-status` key from both modes. PERM's full run precedes health and stamps its own key, so a failed PERM full IS reported.

**P6 (odd rows in perm_case_status): resolved, not a leak, except two rows.** G-200 (118,167 rows), G-300 (6,853) and G-400 (89, all professional-sports employers: MLS, NHL, MLB clubs, filed 2023-06 to 2025-07, 77 re-checked by DOL directly) are real PERM office codes. `MAX(filing_date)=2026-09-03` is two G-200-26246 rows recorded by visitor lookups on Sun Sep 6 at 10:04 AM and 10:40 AM EDT. The two P-100 rows are the only foreign ones: written Wed Sep 2 at 4:55 PM and 5:25 PM EDT, after the guard commit (07577c34, 3:45 PM) and the same-day cleanup, but before the deployment carrying the guard (GitHub deployment 8e20b0d6 created 6:38 PM). They are counted today as a 2-case PERM review stage "DETERMINATION ISSUED" in `review_stages`, `live_census` and `stage_cohorts`, and listed under two employers in `perm_live_recent`.

**P7 (external state): each line refined.**
- "ingest-health all green this morning": true at 9:24 AM EDT and uninformative. `lca-status` is stamped as_of 2026-09-06 with note "0 checked, 0 moved" over a table whose newest filing is Aug 27. Nothing reads any table's frontier; every freshness row measures that a job ran.
- "crons ~3.5h late": measured per workflow, drift is 2 to 12 hours (ingest-health +3h15m to +11h06m; processing-times median 311 min, max 727). The consequence is lost ordering between workflows, not lateness.
- "Fluid CPU $8.77" now has a measured driver: a non-JS crawler hitting `/perm-case-status?case=P-…` at 11 to 100+ requests per minute (PostHog shows 197 human visitors on that path in 3 days), plus the auth proxy running on every request (85 to 93 percent of a 9.9 req/s sample). Entity pages were 2.6 percent of requests.
- "Observability Events $1.32" is unexplained in P7; the documented meter is Observability Plus events at $1.20/M, which only accrue when Plus is on. The memory note "Observability Plus OFF" is probably wrong [medium confidence; dashboard settles it].
- "Email peak 34/day, 0 bounces": Resend's own 15-day metrics say 32/day (Aug 31, weekly digests); conclusion unchanged. But there is no bounce/complaint webhook, and one registered user is silently suppressed at the account level (complaint, Jul 9).
- "Convex logs clean": true for the ~15 to 30 lines the CLI returns without `--success`; a 980-line `--success` log proves every one of the 13 crons fired on Sep 5 and 6 with 0 failures.
- "last build Sat Sep 5 ~10:13 PM": confirmed by `vercel inspect` (10:13:24 PM EDT); every cached page's Age agrees.

**Documentation and memory that the readers found wrong:**
- v2/CLAUDE.md: "A quarterly file only carries its own quarter's determinations" is wrong for PW/LCA; the Qn file is cumulative for the fiscal year (FY2026_Q3 holds 147,226 PW rows vs 249,486 for all of FY2025). "/signup runs on revalidate=86400": measured fully dynamic (`private, no-cache, no-store`, MISS every hit). "Sentry: read the alert emails": no Sentry mail since Apr 17 2026, and the sender is `noreply@md.getsentry.com`, not sentry.io. "Speed Insights was NOT cut": the package is absent from package.json and instrumentation-client.ts says PostHog replaced it on Aug 29. "92,113 expiry rows under two timestamps": it's 137,220 (92,113 + 45,107). Email arithmetic "70/day, 30 left" is the pre-Aug-29 figure; code says 75/25. "Status: NOT FIXED" on glued JSX text contradicts the same file's later section and the root file (it is fixed and gated). "Four vitest projects": there are five (ssr added Aug 31). "37 guides": 33. Case-number guide says the middle digits are fiscal year and day; measured, they are calendar year and UTC day of year (G-100-26240 submitted 2026-08-28T06:12Z; fiscal day 240 would be May 28).
- Auto-memory: "Convex holds only user data, permCases etc. are empty" is wrong: the Aug 30 disclosure run imported 373,939 permCases and 78,600 permEntities documents into prod. "420.5 MB, ~35 years of runway": 1,501.9 MB today. "PostHog replay masking deliberately OFF": since commit 0b9d0e12 (Aug 29 5:36 PM EDT) the init sets `maskAllInputs: true, maskTextSelector: "*"` and recording starts only inside the signed-in app; the privacy page (edited Sep 3) still describes the old behaviour. "browserslist via @serwist/next": now via storybook/webpack/babel devDependencies only; conclusion (build-time only) unchanged. "design standardization: 226 palette classes to zero": public surfaces only; ~880 remain in the authenticated app. "8 lint errors": today 13 warnings, 0 errors.
- Two .planning files and the case-status-mirror workflow header still say the DOL endpoint is reCAPTCHA-gated, a premise refuted on Aug 27.

---

## 2. Confirmed findings

**Every finding below is the reader's own claim and has not been verified by a second agent** (adversarial verification was skipped to save tokens). Each critical and high finding will be verified by probing during the fix: reverting the change and watching the named artefact go red, or reading the log line or DB row the fix is supposed to produce. Findings are ordered by severity, then confidence, then whether they are measured.

### Critical

**C1. PERM discovery has been dead since the Aug 31 run and cannot recover on its own.** (discovery D1/D2/D4, workflows WF-2, health H2, data D1/D2; all measured, high confidence)
- Plain: the nightly process that finds newly filed PERM cases has found nothing since Aug 30 10:52 AM EDT, and by design it never will again.
- Mechanism: day-major probing with a 2-batch dry streak abandoned Aug 28 during the overnight lull; the frontier advances only on a hit; the 5-day window then slid past the frontier's own day code. Also G-100-only candidates and frontier; no 6-digit padding; no wrap at 1,000,000.
- Evidence: "frontier serial 200,246, probing 1500 serials x 5 day codes" then "10 requests, 0 new cases recorded" on Mon Aug 31 12:07 PM, Tue Sep 1 9:27 AM, Wed 8:44 AM, Thu 8:47 AM, Fri 8:41 AM, Sat 7:48 AM, Sun 8:06 AM EDT. Newest prober row G-100-26240-200246; visitor-found G-200-26246-215323 is 15,077 serials past reach. Code: `ingest_case_status_direct.py:183-186, :213-241, :249-260, :270-275, :236`.
- Impact: roughly 6 business days at ~410 PERM filings/day, about 2,500 filings, absent from `perm_case_status`, `perm_live_recent`, the case search, employer pages, `live_census`, `review_stages` and every "Live from the Department of Labor" figure. The PWD/LCA prober seeds its serial windows from G-100 rows, so it's dead too (C2). Structural undercount of ~25 to 30 percent (G-200) even when it worked.
- Fix: rewrite `run_discovery` as a serial-major forward walk that carries the day code: frontier = newest (day_code, serial) across all four G- PK ranges, persisted in `perm_docs['discovery_frontier']` and advanced only by confirmed hits; batches of 25 serials x {G-100, G-200} under `cursor_day`, re-asked under later day codes up to today before being declared "not yet issued"; stop after 2 such batches; format serials `:06d`; wrap at 999,975 to 000000; raise `DISCOVERY_REQUEST_CAP` to 400 and write status "partial" on cap. Recovery: serials 200,247 to ~218,500 under codes 26240 to 26249, ~770 requests, 8 to 9 minutes at PACE_S 0.35, ~2,500 inserts, run from the laptop with the RW token on Tue Sep 8 (DOL is closed Labor Day so waiting costs nothing). Note `workflow_dispatch` has no "discover" mode; a laptop run is the only no-workflow-edit route.
- Touches: `v2/scripts/ingest_case_status_direct.py`, `v2/scripts/test_case_status_direct.py`, `v2/CLAUDE.md`. No Vercel build.
- Verify: tests with a fake lookup (a serial run that changes day code mid-walk is followed; a 200-serial PERM-free stretch does not stop the walk; serial 70,550 renders as 070550; frontier of 999,990 yields 000000 next; frontier picks a G-200 row when newest), each red on revert. After the recovery run: `SELECT MAX(case_number) FROM perm_case_status WHERE case_number LIKE 'G-100-2624%'` moves past 26246, and the log prints "N new cases recorded" with N > 0.
- Best vs asked: the nightly `build_entity_detail.py --live-recent-only` plus the revalidate-live-employers POST after the next full sweep will expire up to 800 employer pages (its own cap), the one Vercel-side cost of catching up. Accept it or skip that night's revalidation.

**C2. PWD/LCA live tables have a 2.4-month hole (Jun 10 to Aug 21) and no new filings since Aug 27/28, because the serial counter wrapped and the backfill has been stuck on day 26161 since Thu Sep 3 4:18 AM EDT.** (PWD-1, PWD-2, WF-7, data D3; measured, high)
- Mechanism: `day_windows()` takes MIN and MAX of a day's known serials; on Jun 10 the counter ran 998,922 to 999,997 then restarted at 1, so the window is (1, 999,997), a million serials. `probe_days` walks `I-200-` from serial 1, burns the 4,500-request cap 225,000 serials in, finds nothing, writes no progress; the chain guard correctly refuses to continue. Separately, daily discovery only probes days for which some table already holds a row, anchored to today, so `day_windows(db, [26243..26249]) == {}` and the log says "discover: 0 requests". Sep 4, 5 and 6 runs each printed exactly that, recorded "ok", and stamped freshness.
- Evidence: `perm_docs.flag_backfill_progress = {"lastDayDone": "26160"}`; run 33729300228 log "request cap 4500 reached inside 26161 (I-200-); resume later … frontier before: 26160 after: 26160"; `pwd_case_status` filings by window: May 18,992; Jun 1 to 9 5,751; Jun 10 to 30 6; July 0; Aug 1 to 21 0; Aug 22 to 31 1,457. LCA: May 75,150; Jul 0. Frontiers: P-100-26240-200540 (Aug 28), I-200-26239-199948 (Aug 27).
- Impact: on the order of 45,000 PWD and 180,000 LCA filings absent (extrapolated from May). `/pwd-cases` and `/lca-cases` render a filings-by-month series that collapses in June and vanishes in July, which a reader takes as "DOL did nothing". Anyone holding a PWD/LCA filed Jun 10 to Aug 21 gets a miss, then a live DOL lookup only while the 2,000/day budget lasts, which H1 shows is spent by bots early each day. The counter wraps again in roughly May to September 2027 (rates disagree: ~3,000/day vs ~2,150 to 2,800/day), so this is a recurring shape.
- Fix: in `day_windows`, split a day's known serials into clusters at any gap > 50,000, window each cluster with EDGE_PAD, clamp when a day's total window exceeds MAX_WINDOW = 20,000 and log `::warning::`; make progress resumable within a day (persist highest serial reached per code). Anchor daily discovery to the frontier, not today: for each day code from frontier_day+1 through today synthesize a window `[next_serial, next_serial + 3,500 + pad]`, stop a day code after 3 all-prefix-empty batches, bound with DISCOVERY_MAX_DAYS_BEHIND = 21. When `windows == {}` with non-empty codes, print `::warning::`, record status "partial" (already in BROKEN_STATUSES) and do not stamp freshness. Then, after Sep 7, dispatch once: `gh workflow run pwd-status-direct.yml -f mode=backfill -f from=2026-06-10 -f to=2026-09-08` (~4,600 requests over 2 legs, 4 to 5 hours, ~1.3M Turso row-writes including index maintenance, ~0.9M if the duplicate indexes are dropped first). Workflow: chain step gets `if: … && !cancelled()`, completion guard before no-progress guard, discover timeout 60m to 90m.
- Touches: `v2/scripts/ingest_pwd_status_direct.py`, `v2/scripts/test_pwd_status.py`, `.github/workflows/pwd-status-direct.yml`, one dispatch. No build.
- Verify: a fixture feeding `day_windows` serials {19, 3484, 998922, 999984} asserts two windows under MAX_WINDOW (red on revert to min/max); after the backfill, `pwd_live_summary.byMonth` carries a 2026-07 entry and `flag_backfill_progress.lastDayDone` >= 26251.
- Disagreement: the workflow reader proposed only "write progress per day code"; the PWD reader found the cause (the wrap window). Both are needed; the wrap fix is the load-bearing one.

**C3. Every monitor measures that a job ran, not that the corpus moved: seven days of zero discoveries, a 10-day-dead LCA feed and a stuck backfill were all green.** (H2, D3 discovery, PWD-4, PWD-8, SEC-4, D7 discovery [measured=false for the DOL-empty-body case]; high)
- Mechanism: `stamp_freshness` fires when a sweep answered > 0 cases (PERM) or when a summary doc wrote (PWD/LCA, including "0 checked, 0 moved"); `as_of` is the run date; discovery yield reaches only a log line; `--discover`-only runs that find nothing write no audit row; a sweep truncated by three DOL failures still records "ok"; a partial PERM sweep stamps fresh after a single successful batch and notes the table size, not rows refreshed.
- Evidence: `data_freshness` lca-status `['2026-09-06', 'Daily', '0 checked, 0 moved', 3]`; `SELECT MAX(filing_date)` = 2026-08-27 (covering index, 1 row); `ingest_runs` #4 to #54 every full row "ok"; ingest-health run 34036010233 (9:24 AM EDT) "lca-status 2026-09-06 0d 3d ok".
- Fix: (a) in `check_ingest_health.py` add `check_frontier(db)`: PERM newest by PK range, PWD/LCA by `MAX(filing_date)`, fail when older than FRONTIER_MAX_DAYS = 5 (measured normal lag 0 to 2 days; a Friday filing after a Monday holiday is 4); (b) record discovery yield in `ingest_runs` unconditionally with `rows_written`, and fail when the 3-day weekday sum is 0; (c) skip the stamp when checked == 0 and discovered == 0; stamp `as_of` from the newest filing the table holds, `max_age_days` 5; (d) `_note` returns None when a sweep is incomplete, and `record_run` gets "partial"; PERM stamps only when checked >= 0.9 x asked with `checked/asked` in the note; (e) log "DOL answered nothing for N requests" distinctly.
- Touches: `v2/scripts/check_ingest_health.py`, `test_ingest_health.py`, both ingest scripts. No build.
- Verify: fixtures for a 6-day-old frontier (must fail) and a 4-day-old one on a Tuesday (must pass); run the checker locally today after the change and confirm it exits 1 naming perm/pwd/lca frontiers at 9, 9 and 10 days.

### High

**H1. A robots-ignoring crawler hits `/perm-case-status?case=P-…` tens of thousands of times a day, spends the 2,000/day DOL discovery budget by early morning, and every refused hit still costs a Turso write, a read and a dynamic render.** (T1, EXT-1, data D5, T6 [measured=false]; measured, high)
- Evidence: `perm_docs` counters `pwd_discovery_budget_2026-09-04 = 74,220`, 09-05 = 32,255, 09-06 = 14,885 by 5:31 PM EDT; `lca_… 09-04 = 11,492`; PERM `discovery_budget_*` 1 to 5/day. `vercel logs --level error` 100/100 rows `[pwdDiscovery] daily budget refused P-100-…` at ~20/min; robots.txt line 11 `Disallow: /perm-case-status?`; refused numbers are 2023/2024 day codes the site links from disclosure listings. Code: `caseDiscovery.ts:127-144` increments then checks; `flagCases.ts:414-419` discovers on any live-table miss even when `pwd_cases` already holds a final determination; `client.ts:157-160` can retry the non-idempotent upsert (double charge).
- Impact: a real beneficiary with a pending P-/I- number gets "no record" for the rest of each UTC day. ~2.2M Turso writes/month from a counter; ~59k dynamic invocations/day on one path; error logs are 100 percent this line.
- Fix, no build: Vercel Firewall rule on path `/perm-case-status` with `case` present (rate limit ~20/60s per IP, challenge non-browser UAs), and enable Bot Protection; read the Firewall tab for the UA (CLI logs carry none). Code, next batched deploy: check-then-increment and charge only when a DOL request is issued; skip `discover` when `lookupDisclosed` already returns a final determination; log the refusal once per UTC day; optionally require a form-origin signal for discovery.
- Touches: Vercel dashboard (no build); `v2/src/lib/turso/caseDiscovery.ts`, `flagCases.ts`, `PwdStatusResult.tsx`, `LcaStatusResult.tsx` (build).
- Verify: `pwd_discovery_budget_<day>` stays under ~500 after the rule; a unit test asserting no `exec` call when the cap is reached (red on revert); the Firewall tab shows blocked count.
- Best vs asked: a WAF rule before Monday changes the baseline; without it Monday measures a crawler, not the product. Owner must say which they want measured.

**H2. The Sunday PWD/LCA full sweep can never finish, burns ~300k writes for ~54 changes, and Monday's pending run will most likely hide the failure from ingest-health.** (PWD-3, PWD-5, PWD-6, WF-1, H1 health, SEC-3, data D7; measured, high)
- Fix: PWD gets a 180-day filing window (`is_final=1 AND filing_date >= today-180`, index-served, ~47k rows, ~27 min); LCA's withdrawn-after-certified tail is unbounded by filing date (measured 129 to 243 days, validity up to 3 years), so it gets a daily 1/7 slice by serial modulo instead of a weekly full (~44k rows, ~25 min added to the pending run) or a 1/28 monthly slice if 25 min/day of DOL traffic isn't wanted. Keep `last_checked_at` stamping (it IS read: `PwdStatusResult.tsx:278`, `LcaStatusResult.tsx:198`, `FlagCaseBrowser.tsx`, `rfi.ts:68`; the workflow reader's "stop writing it" would break the "Checked" column, the PWD reader's grep is the stronger evidence) but shrink the population. Register a SIGTERM handler that flushes and records PARTIAL. Interim, workflow-only: Sunday runs `--full --program pwd` (56 min) until the script lands. Health: key on (filename, mode) with `--${MODE}` carried into the failure hook via `GITHUB_ENV`; stamp `pwd-status-full`/`lca-status-full` (max_age 10); rewrite `test_ingest_health.py:107-119` so a `--pending ok` does not clear a `--full failed`; add `workflow_run` completion triggers to ingest-health so ordering stops being queue luck.
- Touches: `v2/scripts/ingest_pwd_status_direct.py`, `check_ingest_health.py`, `test_ingest_health.py`, `.github/workflows/pwd-status-direct.yml`, `case-status-direct.yml`, `ingest-health.yml`. No build.
- Verify: `sweep_runs` gets an lca full row with complete=1; Sunday log ends with "recorded sweep … COMPLETE" for both programs inside the timeout; the health fixture with a later `--pending ok` still exits 1.

**H3. The case-status page tells a pending beneficiary their case "has not been looked at since" July, or "7 days ago", when the sweep verified it this morning.** (LIVE-1, data D4; measured, high)
- Evidence: served `/perm-case-status?case=G-100-26240-200246`: "Status seen | August 30, 2026 (7 days ago)"; `sweep_coverage` doc: full sweep finishedOn 2026-09-06, asked 414,358, answered 414,346. `perm_case_status.last_checked_at` samples: June 2026 filings 488/500 stamped 2026-07; G-100-26230 range 500/500 NULL. The ingest's own docstring (`:37-41`): "THE SITE WAS QUOTING last_checked_at BACK AS IF IT WERE OURS. This script has never written that column … 66,771 pending cases carried a 2026-07 date and 12,187 carried none". Readers: `caseLookup.ts:132,166,260`, `casePosition.ts:245-252` (STATUS_STALE_DAYS=14), `CaseStatusResult.tsx:171-182`. The same column drives AGE_DAYS in `rfi.ts:148` and the Python `_AGE_DAYS`, so stage medians are short by the stamp's age and ~12k NULL rows are excluded.
- Impact: the product's flagship page tells its core audience the site is stale, on every pending case older than ~6 weeks.
- Fix: for pending PERM rows take the check date from `perm_docs['sweep_coverage'].finishedOn` (React-cached point read, 8-day cutoff), relabel "Checked by our DOL sweep <date>" and "Status unchanged since <date>"; compute AGE_DAYS from filing_date in both TS and Python (pinned identical by the existing doc test); rename or retire the column from reads.
- Touches: `v2/src/lib/turso/caseLookup.ts`, `src/lib/casePosition.ts`, `src/components/tools/CaseStatusResult.tsx`, `src/lib/turso/rfi.ts`, `scripts/ingest_case_status_direct.py`. Build required.
- Verify: a component test feeding a July `last_checked_at` and a today `finishedOn` asserts the rendered date is today (red on revert); after deploy, curl the same case and read "Checked … September N".

**H4. `/perm-rfi-audit` prose says the analyst queue "was not" read this week while the tile beside it says "checked Sep 6".** (LIVE-2; measured, high) Hardcoded `<p>` at `perm-rfi-audit/page.tsx:218-222` from the weekly-sweep era. Fix: delete it or render it only when the ANALYST REVIEW stage's `seenTo` is older than N days. Build required. Verify: curl shows one freshness claim.

**H5. Tests workflow red for 25 pushes; vitest has not run in CI since Sep 3.** (WF-3, LC-1, DOC-2; measured, high) Fix: add `"attorney_name": None, "attorney_slug": None` to the 6 expected dicts in `test_flag_disclosure.py:220-278`, better with a `LAWFIRM_NAME_BUSINESS_NAME` fixture column so the mapping is exercised; split the three scripts into named steps; wire `test_entity_identity.py` (run by no workflow, LC-3); swap the README's static "6,444 Passing" shield for the workflow badge. No build (README is skipped by the ignore pattern). Verify: next Tests run shows step "Run full test suite" executed and green; the run's file count is ~335. Disagreement on date: the workflow reader's run id (10:47 PM EDT first red) beats the doc reader's "since at least Sep 4 6:35 AM".

**H6. The live `perm_entities` contradicts `perm_entity_alias`, and the Sep 20 cron will reproduce the damage.** (S1; measured, high) 62 dangling alias targets, 61 alias slugs that are also live entities, because `turso_migrate_public.py:49` DROPs and re-slugs in volume order without reading the alias table, while `rebuild_entity_identity.py` (in no workflow) wrote the aliases. Impact: 62 URLs that were canonical Aug 27 to 30 now 404 via the alias path; "absorbed N spellings" wrong on 61 pages. Fix: make the scheduled loader use `fold_typos()+assign()` against live rows and write aliases; `turso_migrate.py` takes its slug map from the result; rerun the identity rebuild once; delete `rebuild_entities.py`. Gate the Sep 20 cron until then. No build. Verify: the LEFT JOIN count returns 0 after the run and again after a dispatched disclosure run.

**H7. Every disclosure run writes ~455k documents into Convex tables nothing reads.** (S2; measured, high) `convex import --table permCases --replace` (373,939 docs), permEntities (78,600), permWageStats, permDisclosureStats, visaBulletins: zero `api.*` readers in src or convex; the workflow's own comment says the site reads Turso. Convex free tier (1M calls, 0.5 GB storage, 1 GB I/O) has tripped once and disabled the deployment. Fix: remove those workflow steps, keep `uscisI140:storeStats` (read by two tool pages); retire `store_cases.py` and the Convex half of `store_entities.py`; clear permCases from the dashboard when convenient; leave `convex/schema.ts` alone during the freeze. No build. Verify: next run log has no "Added N documents to permCases"; Convex Usage falls.

**H8. `record_ingest_failure.py` prints "recorded failed" whether or not the row landed, and a missing credential raises SystemExit past its `except Exception`.** (H3 health; measured, high) Fix: `record_run` returns True/False, the hook reads back the row and prints `::error::` when absent, `except BaseException`. No build. Verify: a resilience fixture whose Turso stub raises asserts the `::error::` line.

**H9. Privacy policy omits two live third parties and promises a GPC opt-out that covers only PostHog; it also describes three data flows that no longer exist.** (SEC-1, SEC-2; measured, high) Ahrefs analytics in the root layout (unconditional), Senja widget on the homepage; policy says GPC disables "all analytics". Sentry Session Replay was removed Aug 29, PostHog masks all text and records only in the app, Speed Insights package is gone; the policy says the opposite of each. Fix: name Ahrefs and Senja; gate the Ahrefs script on `navigator.globalPrivacyControl` or narrow the sentence; rewrite sections 7, 8, 13; decide whether masked replay is intended (memory says unmasked was Adam's call). Build required, copy-level. Verify: `curl /privacy | grep -c Ahrefs` >= 1; a test greps the policy for "Session Replay" under Sentry and fails.

**H10. Front-end craft floor breaks on public pages: an infinite pulse under the `/for-attorneys` h1, and lime `text-primary` as running text at 2.05:1 on 86 light-mode sites.** (FE-1, FE-2, LIVE-6; measured, high) `.pulse-dot` 2s infinite (globals.css:538-546); `#2ECC40` on `#FAFAFA` = 2.05:1, while the theme-swapped `--primary-text` (#1D8229, passes) exists and is bypassed on privacy/terms links (no underline at rest), CaseWall 10px labels and more. Fix: remove the class and keyframes; replace `text-primary` with `text-primary-text` on text/link elements; underline at rest. Build required. Verify: a built-CSS gate greps for `infinite` outside the spinner/skeleton allowlist; a contrast script over the token pairs.

**H11. Measured request rate is ~8 to 10 req/s and 85 to 93 percent of it is the auth proxy running on every page, CDN hits included; entity pages are 2.6 percent.** (T2, EXT-3; measured counts, [medium confidence] on the daily extrapolation from two Sunday-afternoon minutes) `src/proxy.ts:140` matches every non-file path; Vercel runs middleware before the ISR cache. Derived: ~16M invocations/month against Hobby's 1M, ~340 CPU-hours against 4, and Hobby's terms exclude commercial use. Fix: narrow the matcher to `(authenticated)`, `/login`, `/signup`, `/api/chat`, `/api/google`; re-measure with a 10-minute log sample before choosing a plan. Build required (proxy.ts). Verify: `vercel logs --json` sample shows serverless-middleware < 20 percent of rows for public paths.
- Best vs asked: the plan to cut entity pages to fit Hobby targets the wrong driver; the bill is traffic-shaped (crawler plus proxy), not page-count-shaped.

### Medium

| # | Finding (source) | Mechanism and evidence, briefly | Fix, touches, build | Verify |
|---|---|---|---|---|
| M1 | Two leaked P-100 rows sit in `perm_case_status` and `perm_live_recent`, counted as a 2-case PERM stage (D5, T5, D6, DOC-1, H8; measured) | Written 4:55 and 5:25 PM EDT Sep 2, between guard commit and deploy; `review_stages` lists ('DETERMINATION ISSUED', 2); pendingTotal 94,014 includes them | Manual `DELETE … WHERE case_number IN (…)` on both tables (4 PK deletes); add a three-range prefix invariant in `main()` that `SystemExit`s naming strays; no build | Invariant query returns 0; next `review_stages` doc has 9 stages |
| M2 | Serial counter wrap deadlock is deterministic, next ~Jun to Sep 2027 (D4 discovery; measured) | Folded into C1 (padding, wrap, frontier by hit order) | scripts | Test: frontier 999,990 yields 000000 next |
| M3 | Two pending totals for one cohort: 94,023 on `/perm-queue` and `/perm-case-status`, 94,014 on `/perm-rfi-audit` (LIVE-4; measured) | Fixture-employer exclusion applied in `review_stages` but not `live_census` (ingest `:643-645`) | Exclude the fixture in `write_live_census`; scripts only, no build | Both docs' totals equal after the next sweep |
| M4 | Five duplicate indexes on `pwd_case_status`/`pwd_case_events` (PWD-7, S11, D9; measured) | `pwd_status_*` family from an older name; every insert writes 9 entries not 5 | Manual `DROP INDEX` x5, then EXPLAIN in a fresh request; add `*_events_recent` to `table_ddl`; no build | EXPLAIN still `SEARCH … USING INDEX pwd_case_status_final_filed` |
| M5 | FLAG quarterly files are cumulative per fiscal year and the loader is not diffed: the Q4 files (Oct/Nov) cost ~7.8M writes, ~10 to 11M in that month with sweeps (S4; measured) | `INSERT OR REPLACE` on every row, 9 indexes, per-file load keys bypass the sha short-circuit | Diff by fingerprint bounded to the fiscal year, skip unchanged (~95 percent); fix the CLAUDE.md sentence; no build | Run log prints "N unchanged skipped, M written" with M << N |
| M6 | Monthly perm-disclosure cron (first fires Sun Sep 20 ~3 AM EDT plus drift) has never run scheduled and rebuilds everything with no change gate, including a DROP of `perm_entities` the live site reads and the Convex writes in H7 (S5, WF-5, S6; measured) | 15 runs, all dispatch; `contentHash` computed and compared by nothing; the 20th cron was added for a bulletin the archive can no longer supply | Gate store/load/build/revalidate on `cases_meta.contentHash`; delete or condition the 20th cron; make `build_entity_detail` full mode upsert-then-prune and `record_run`; move the `daily_decisions`/`entities` stamps into `turso_migrate_public.py`; delete `backfill_permtrack.py` (6-value insert into a 7-column table, third-party source); no build | Dispatched run on unchanged data logs "unchanged; skipping" and writes ~0 rows; `data_freshness.entities` advances |
| M7 | I-485 monthly ingest is blocked at the runner's IP, has no failure path, health stays green until Oct 19 (H4, WF-6, S3; measured) | 4x 403 from the runner, 200 from the laptop with identical headers; no hook, no failed row, stale max_age 75 | Add the failure hook, wrap fetch in try/except → `record_run(failed)`, retry crons on the 13th and 20th; run `scripts/ingest_i485_inventory.py` from the laptop this week to capture the September file; no build | `ingest_runs` gains an i485 row; `data_freshness.i485-inventory.as_of` moves to the Sep release |
| M8 | Three freshness rows have no living writer and will alarm in November/January for datasets nothing stamps (H5 [medium confidence]; measured) | `daily-decisions` (as_of 2026-06-30), `entities`, `uscis-i140-times` written only by `backfill_permtrack.py` | Stamp the first two from the quarterly loader (M6); delete the third row manually; no build | Checker lists 19 datasets, none orphaned |
| M9 | `pwd` status vocabulary drift: RETURNED UNPROCESSED (274) and CENTER DIRECTOR REVIEW AFFIRMED DETERMINATION (1) counted pending; PERM 'DENIED - BALCA DISMISSED' (9) pending forever (PWD-9, data D8; [medium confidence] on the DOL semantics) | `is_final()` defaults unknown to pending; `pwdCases.test.ts` pins the TS set to the Python file | Add to `PROGRAMS['pwd']` finals and pendings, `FINAL_STATUSES`, `pwdCases.ts`, `flagCaseNumber.ts` in ONE commit; one-off UPDATE of 284 rows; build required | `pwd_live_summary.pending` drops by 275; CI green |
| M10 | Cron drift 2 to 12 h voids every "runs after X" ordering; both DOL sweeps overlap in US business hours (WF-4, H7; measured) | GitHub queues schedules independently | Chain pwd-status-direct and ingest-health on `workflow_run` completion, keep crons as floors; fix EDT-only labels to "09:40 UTC (05:40 EDT / 04:40 EST)"; no build | `gh run list` shows pwd starting within minutes of PERM finishing |
| M11 | `/signup` renders dynamically on every hit despite `revalidate=86400` (LIVE-3; measured) | Cookie read in the (auth) layout; `/login` escapes via `force-static` | Add `export const dynamic = "force-static"` to signup; build required; test signed-in behaviour on `pnpm dev` first | `curl -I /signup` shows HIT with age > 0 |
| M12 | A perm_docs doc older than 8 days silently re-enables the 414k-row scans on the dynamic lookup path; health gate is 3 days (T4 [measured=false]) | `parse*Doc` returns null, callers compute live | Return the empty state on a stale doc; keep live SQL only in the ingest; build required | Unit test: stale doc → null result, no SQL issued |
| M13 | `/api/perm-wages` is unauthenticated, `revalidate=0`, no Cache-Control, and one call walks the status slice ~5 times (~1.5M rows) (T3; measured plans, latent) | CTE re-evaluated per scalar subquery; 0 hits sampled | `s-maxage=604800`, one-pass query; build required | `curl -I` shows cache-control; EXPLAIN shows one SEARCH |
| M14 | Email budget ledger omits at least seven sending paths that share the 100/day cap (CX-1, AUTH-10; measured counts) | Contact form 25/day, weekly digest ~30 to 45 on Mondays, deadline digest, nudges, welcome, support forward, test email all uncounted; measured peak 32 to 34/day so no outage today | Add lines and global caps (contact 10, support 10, nudges 5/run, test 1/10min); route OTP/reset through `sendEmailWithRetry`; convex, build required | Ledger sums to <= 80 with auth reserve 20; a test asserts the caps exist |
| M15 | Support inbound forward has no cap and no idempotency; Resend webhook retries double-forward (CX-2; measured) | `email.received` branch lacks the svix dedupe the contact branch has | `by_resend_email_id` index, skip duplicates, global 10/day forward budget; convex + schema, build required | convex-test: same event twice → one forward |
| M16 | No bounce/complaint webhook; one registered user silently suppressed since Jul 9 (CX-3, EXT-6; measured) | Resend suppresses at account level but returns an id, so sends look successful | Add bounce/complaint/suppression events to the webhook (dashboard, no build) and handle them in `http.ts` (build): unsubscribe alert rows, flip `emailNotificationsEnabled`, surface in admin | A test bounce event marks the profile |
| M17 | `checkAuthRateLimit` is a public mutation keyed on a caller-supplied email; 30 calls per 15 min lock a chosen attorney out and one attacker-made `ip_strike` auto-suspends them 24h (AUTH-2 [measured=false]) | Second entry point skipping the HTTP layer; corroboration looks for ANY strike | Make it `internalMutation` invoked from the server side of auth; tie the strike to the request IP; convex + src, build required | Reproduce on DEV before and after (manual step listed) |
| M18 | Suspension is enforced only in the login page's client code (AUTH-3 [measured=false]) | `getCurrentUserId` never reads `suspendedAt` | `requireActiveUser` in `convex/lib/auth.ts`; build required | convex-test: suspended user's query throws |
| M19 | Daily reminder query collects four whole tables; case list hard-caps at 1,000 (AUTH-4 [measured=false, medium confidence]) | Growth cliff at ~10k cases or many reminder rows; fine at 139 users | Fan out per user; index notifications by (type); paginate `cases.list`; build required | Insights tab shows no read-limit warnings after a seeded 5k-case test |
| M20 | `importCases` has no rate limit and no row cap; large CSVs fail atomically with an opaque error (AUTH-5 [measured=false]) | One transaction; Convex 8,192 writes/16,384 reads | Cap 500/mutation, chunk on the client, `rateLimiter.limit`; build required | Test: 501 rows → ConvexError naming the cap |
| M21 | AI chat AUTO mode lets injected text in tool output mutate cases; no per-user quota (AUTH-6 [measured=false, medium confidence]) | Tool results inserted as plain text; `confirm` tools auto-execute in AUTO | Delimit tool output with a system rule; downgrade mutating tools when a searchWeb result is in the step; per-user daily budget; build required | Test: a searchWeb fixture containing "update case X" produces no updateCase call in AUTO |
| M22 | Case form rejects the last valid recruitment date when the 180-day span crosses spring-forward DST, US zones only (AUTH-1; measured with `TZ=America/New_York node`) | `new Date(iso)` UTC midnight + `setDate` local + `toISOString`; 2026-02-10 +180 → 08-08 vs correct 08-09 | Use `@/lib/perm` composites and ISO comparison in `case-form-schema.ts:490-518`; build required | vitest under `TZ=America/New_York` with first=2026-02-10, method 2026-08-09 → no issue (red on revert) |
| M23 | Small-label system below the measured floors (10 to 11px, tracking up to 0.18em) on the case card, number plate, alert form; 363 explanatory `<p>` at 14/12px; content-module Motion ignores reduced motion and runs 0.6 to 1.2s; app-side infinite pulses; hero image declares 800x600 for a 1312x816 file (CLS); public lookup skeleton `animate-pulse`; em-dash in the welcome subject and 59 dash placeholders where "not published" belongs (FE-3 to FE-9; measured) | Static analysis; contrast/legibility not rendered on a phone this pass | Floors (14px, <= .12em), `text-base` for running prose, `<MotionConfig reducedMotion="user">` in the public layout, delete the pulses, fix width/height, `skeleton-pulse`, subject "Welcome: …", an `<Unpublished />` word; all build required | Static gates: `text-\[(1[0-3]|[0-9])px\]`, `tracking-\[0\.(1[3-9]|[2-9])`, `"—"` literals, `infinite` in built CSS; declared==actual image size test |
| M24 | Observability Plus appears ON ($1.32 metered events in 6 days) (EXT-2 [medium confidence]) | Plus is on by default for teams upgraded after Apr 3 2026 | Dashboard: Billing → Observability Plus, exclude `perm`; no build | Next `vercel usage` shows Observability Events $0 |
| M25 | Dependabot fires Mon Sep 7 5:00 AM EDT and each branch gets a Vercel preview build on the baseline day (EXT-4; measured) | `day: monday, time: "09:00"` UTC; ignore script has no branch check; Aug 31 produced 6 preview builds, 2 failed on the AI SDK peer gap | Change `day` to tuesday and add `case "${VERCEL_GIT_COMMIT_REF:-}" in dependabot/*) exit 0;; esac` to `vercel-ignore.sh`; both paths skipped, no build; do it before 5 AM Monday | `npx vercel ls --environment preview` shows nothing new Monday |
| M26 | Sentry alert mail has not reached a human since Apr 17 2026; the documented sender is wrong (EXT-5 [medium confidence]) | Alert rule/target changed or mail goes elsewhere; API token is release-scoped (403) | Dashboard: confirm the issue-alert rule and notification email; fix CLAUDE.md sender; no build | A test event from `/api/sentry-check` lands in the inbox |
| M27 | No off-platform copy of the irreplaceable observation tables (events, runs, processing_times, visa_bulletins, perm_docs); recovery rests on Turso PITR (24h on Free) and an unconfirmed Convex backup (SEC-5; measured) | Every writer is `INSERT OR REPLACE` with an RW token | Weekly Actions job dumping the small tables to a private repo/artifact; `npx convex export --prod` now and monthly; record plan and PITR window in CLAUDE.md; no build | The artifact exists and restores into a local SQLite with matching row counts |
| M28 | `pwd_cases` history loaded after the current quarter overwrote 87 rows; count mismatch triggers a 147k-row reload on Oct 10 (S7 [medium confidence]) | `INSERT OR REPLACE` with no newest-decision-wins rule | `ON CONFLICT … WHERE excluded.decision_date >= decision_date`; reconcile on presence-or-superseded; no build | Oct 10 log does not say "reloading" |

### Low and info (consolidated; one line each, source in parentheses)

- Case-number guide says fiscal year/day; it's calendar year and UTC day (D6). Content fix, build. Verify: the guide's example decodes G-100-26240 to Aug 28.
- `--discover`-only runs that find nothing write no audit row; a DOL 200-with-empty-body reads as "no filings" (D7 discovery [measured=false]). Folded into C3.
- The PERM pending pass records "pending: 414,358 cases" while it checked 94,148 (WF-11). Use `asked` in the note.
- `--discover` timeout 60m < its own cap (PWD-11); backfill chain step skipped on exit 124 and guards in the wrong order (PWD-10). Workflow-only, folded into C2.
- `mismatch` from the live-recent rebuild isn't a broken status; surfaces 3 days later (H6 [measured=false]). Add to BROKEN_STATUSES.
- Summary docs re-aggregate both tables 5 times per run (~2M rows/run, ~65M/month, 2.6 percent of a 2.5B allowance) (PWD-12). Leave.
- `build_entity_detail.py --live-recent-only` reads ~1.25M rows twice a day (~75M/month) (S12). Make the post-pending pass incremental; optional.
- `cross_check_sources.py` calls permtrack.app's API daily and counts `perm_cases` (374k rows/day) (S13). Use `cases_meta.totalCases`; move to the monthly workflow or drop.
- `PAGE_FLOOR = 3` in Python no longer mirrors `MIN_TOTAL_FOR_PAGE = 5` (S9). Set to 5 and assert cross-language.
- Visa-bulletin provenance says "gaps via permtrack.app mirror" while 0 rows are mirror-sourced (S10). One constant.
- FLAG loader has no core-column guard (S8 [measured=false]); flag-disclosure downloads both files to hash them and expires 12 pages twice on no change (WF-8). Add the guard; emit `changed=` to `$GITHUB_OUTPUT`.
- `vercel-ignore.sh` builds for stories, `.storybook`, `test-utils`, `tests/e2e`, `__fixtures__`, playwright/eslint configs (WF-9). Extend the pattern; probe both directions.
- 7 workflows lack `timeout-minutes`; perm-disclosure has no inner `timeout` or failure hook; every action floats on a mutable tag (WF-10, SEC-9, EXT-9). 140 open code-scanning alerts, the two in app code are false positives; two `${{ inputs.mode }}` shell interpolations should move to `env:`.
- "No law firm" is `''` in `perm_cases`, NULL in pwd/lca (D10 [medium confidence]); one LCA row with wage 11,111,111 and 5 percent untrimmed employer names (D11). Normalise at write; cap wages in the day feed.
- `revalidateTag('data-freshness')` targets a tag nothing sets; one real tag (`entities`) exists (T10 [measured=false]). Delete or repoint.
- Entity name search walks the whole kind per `q` (T7); `getRfiOccupations` reads the RFI window twice per render (T8 [measured=false]); two discovery-writer inconsistencies (T9 [measured=false]).
- Re-engagement stamps "nudged" before the send resolves (CX-4 [measured=false]); `sendTestEmail` unlimited (CX-5); `/queue-alert/subscribe` forwards unsliced strings (CX-6); nine tables without pruners, `toolCache.cleanExpired` never scheduled (CX-7); four cron reads scan whole tables (CX-8); `recordAlert` has no compare-and-set (CX-9 [measured=false]); crons.ts names the retired mirror (CX-10).
- Alert budget charged before the send, never refunded (SEC-6 [medium confidence]). Dead credentials: RESEND_API_KEY, TAVILY, BRAVE in Vercel with zero readers; two "Onboarding" Resend keys from Nov 2025 (SEC-7). Turso tokens never expire and the RW one lives in three places, no rotation runbook (SEC-8). Terms bind "PERM Tracker", not a legal entity (SEC-10).
- Quiet-hours suppression drops the day's reminder instead of deferring (AUTH-7 [measured=false]); the only Playwright spec asserts a scaffold page that no longer exists and runs nowhere (AUTH-8, LC-2); reset-password code field lacks `autocomplete=one-time-code` (AUTH-9); three hand-rolled copies of canonical date arithmetic (AUTH-12); account deletion leaves the email's alert subscriptions (AUTH-11 [measured=false]).
- ESLint 13 warnings, 0 errors, 4 exhaustive-deps with runtime potential (LC-5); pyflakes 4 unused names, `nonlocal sent` worth a look (LC-6); no global mock reset by design, do not flip it (LC-7); src tests invisible to the typechecker by config (LC-8); priority-date calculator fetches uscis.gov at render time (LC-9 [medium confidence]).
- Emoji in guide callouts and every transactional email (FE-10); opacity-derived muted text fails only in dark (FE-11); DESIGN_SYSTEM.md documents tokens that don't exist (FE-12); two tinted shadows (FE-13); dead infinite-animation CSS shipped on every page (FE-14); FormHelpPopover hardcodes 64px and 100vh (FE-15); trust badges and "nightly check" copy are literals that can't go false, and per C1/C2 the nightly-check sentences on `/pwd-cases` and `/lca-cases` are false today (FE-16 [inferred from C1/C2]); duplicate attorney H2 on the homepage and Title Case on `/for-attorneys` (FE-17 [medium confidence]); ~880 palette classes in the app, urgency tokens fail contrast in dark (FE-18).
- `/tools/i140-trends` description 163 chars (LIVE-5); em-dash null glyphs (LIVE-7); trailing-slash duplicates by design (LIVE-8); `/tools/i485-queue-position` ships "Loading queue figures…" in HTML (LIVE-9 [medium confidence]).
- Docs: reCAPTCHA premise in two .planning files (DOC-3); eight Aug 27 commits authored by the personal handle (DOC-4, owner decision, no rewrite during the freeze); email arithmetic, project count, glued-text contradiction, EDT-only cron labels, guides count, loading.tsx claim, `isEmailBlocklisted` name, `src/app/sitemap.ts` path, version 2.0.0 vs 0.1.0 (DOC-5 to DOC-9, LC-4).
- Turnstile `verify_failed` 25 → 116 → 193/day from 1 to 2 clients (EXT-7): Turnstile holding, no change. PostHog error tracking: two real front-end defects (`.waiting` undefined, 20 occurrences; call stack, 10), rest noise (EXT-8 [medium confidence]). Two Dependabot highs are dev-only (EXT-10). PostHog replay captures request/response bodies with masking off (EXT-11): decision, not code.

---

## 3. What grows forever

Turso: 28 tables, **1,501.9 MB** today (384,485 pages x 4,096; was 420.5 MB on Aug 27, the difference being the FLAG tables). Plan caps from turso.tech/pricing: Free 5 GB / 500M reads / 10M writes per month; Developer ($4.99) 9 GB / 2.5B / 25M. Which plan is in force could not be read (no CLI, no platform token).

| Table | Rows today | Writer, cadence | Pruner | Measured growth | Limit it meets | When it matters |
|---|---|---|---|---|---|---|
| lca_cases | 437,496 | flag loader, monthly, INSERT OR REPLACE | none | +~580k/yr (437k in 3 quarters) | writes cap on Q4 month (M5) | Oct/Nov 2026 |
| pwd_cases | 634,638 | same | none | +~250k/yr (FY24 237,926, FY25 249,486) | same | Oct/Nov 2026 |
| perm_cases | 373,939 | turso_migrate, monthly, diffed | none | +~130k/yr | storage only | years |
| perm_case_status | 414,358 | direct sweep + web discovery | none | +~110k/yr (7k to 10k filings/month) | storage; read cost via docs | years |
| lca_case_status | 309,401 | prober/sweep | none | +~600k to 900k/yr (Mar to May: 45k/82k/75k per month) | Sunday sweep time (H2), storage | now (sweep), storage 2029+ |
| pwd_case_status | 96,009 | prober/sweep | none | +~200k/yr (16k to 19k/month) | as above | now |
| perm_case_events | 150,489 | sweep | none | +~1,250/day (~450k/yr; 137,220 of today's are the Aug 28 to 29 backfill) | storage; day-feed reads bounded | years |
| pwd/lca_case_events | 1,579 / 812 | sweep | none | ~300/day combined | none | years |
| perm_docs `discovery_budget_*` and `pwd_/lca_…` | 17+ keys | web path, +3/day | never pruned | +3 keys/day | none (small) | never, but prune |
| ingest_runs / sweep_runs | 57 / 14 | ~5 to 7 rows/day | none | KB/yr; checker's unindexed `finished_at >= ?` scan | none | years |
| i485_inventory | 9,945 | monthly | none | +~30k/yr | none | years |
| processing_times | 4 | daily, keyed on DOL as-of | none | +~50/yr | none | never |
| daily_decisions | 954 | sweep (its own source only); dol-disclosure half frozen at 2026-06-30 | partial | +1/day | stale, not growth (M6) | after Q4 file |
| Convex permCases / permEntities | 373,939 / 78,600 | disclosure run, replace | none | rewritten wholesale each run | 0.5 GB storage, 1 GB I/O free tier (H7) | Sep 20 cron |
| Convex auth tables, auditLogs, apiUsage, marketingEvents, supportEmails, contactMessages, toolCache | hundreds | app | none (CX-7) | single-digit rows/day | 8k-row read limit | years |
| Vercel ISR cache | n/a | every deploy cold-starts it | 31-day eviction | $3.23 on a 5-deploy day (measured Sep 1) | $20 credit | every deploy |

Rate labels: FLAG and status-table rates are measured from row counts by month; the "years" horizons (Free 5 GB ~mid-2029, Developer ~2032 at ~1.2 GB/yr) are inferred from ~0.5 KB/row all-in and are the growth reader's own [medium confidence].

Monthly write budget, inferred from measured statement counts: baseline ~3M (Sunday sweeps ~1.75M, monthly rebuild ~1M, daily diffs ~0.1M) + the crawler's counter upserts ~2.2M (H1) + the FLAG Q4 month ~7.8M (M5) = a month that exceeds Free's 10M and spends 40 to 50 percent of Developer's 25M. Reads: this side ~90M/month (live-recent 75M, summary docs 65M, cross-check 11M); the site's own reads were the 7.75B invoice and were fixed by EXPLAIN work on Sep 2.

**What does NOT grow:** perm_live_recent (137,085, membership-bounded), perm_entities (78,600, rebuilt), perm_entity_facets/pending (rebuilt), perm_wage_stats (2,190, +~400/yr), data_freshness (20), visa_bulletins (84, +1/month), i140_trends (66), rfi_funnel (1, frozen), perm_month_stats (35, frozen orphan with one reader at `publicData.ts:769`), mirror_progress (2, frozen), the Resend contact list (60), Convex users (139) and alert rows (23 case, 6 queue, 0 to 2 bulletin), the sitemap (13,761 URLs, floor-bounded), rateLimits (24h pruner), notifications (90-day pruner), conversations (90-day).

---

## 4. Checked and sound

**PERM discovery and sweep (scripts, workflow).** Cron/mode mapping correct; full pass 62 min under `timeout 105m`/120; concurrency serialises runs; failure recorder present and credentials removed in an `always()` step; DOL over-return asserted, 4x retry with backoff, 3-failure stop, flush every 40 batches; last full asked 414,358 answered 414,346 (12 missing, stable) COMPLETE; pending pass skips discovery; discovery inserts are `INSERT OR IGNORE`, exact-match, idempotent; frontier query uses the PK covering index; year-boundary day codes pinned; G-400 is a real PERM prefix; `caseDiscovery.ts` guard runs before the budget write, exact match only, every failure branch logged and degraded, no event row; its test pins FINAL_STATUSES parity by reading the Python file; `test_case_status_direct.py` wired in CI; `perm_live_recent` indexes present.

**PWD/LCA prober (scripts, workflow).** Failure hook fired 2 s after the kill and wrote the row; PWD full recorded complete; pending WHERE index-served; `is_final` stored as INTEGER (the 2026-08-29 fix holds); PK-range serial reads (42 tiny reads); `substr(case_number,13)` correct for every prefix shape; schema has all 13 columns; DST-free arithmetic; year boundary and the non-reset at New Year handled; claimed-serial drop by design; over-return SystemExit; reconciliation guards on every doc; two concurrency groups; no-progress guard did its job; writes batched; pace 0.57 req/s; audit inserts never retried; events tables consistent (0 filing-date mismatches on all 39 final-to-moved cases).

**Health and audit trail.** `parse_as_of` handles date, month, quarter; future-dated as_of guarded by the run budget; empty table exits 2, unparseable exits 1; both checks run before any early return; cancelled deliberately not broken; hook reads env vars, not `.env.local`; `record_run`/`record_sweep` never retried; `sweep_runs_cover` serves `last_complete_sweep` without a sort; data-moved stamps take as_of from the source (processing-times = DOL's own stamp, perm-cases = max decision_date, disclosures = file's last decided date); all scheduled writers write 7 values.

**Other workflows and deploy gating.** Deploy freeze held (last deployment Sat 10:16 PM EDT; two .planning pushes Canceled); `vercel-ignore.sh` verdicts correct in both directions for every tested path; case-status-direct 14/15 green, the Sep 3 failure was SQLITE_NOMEM and the hook was added the same afternoon; flag-disclosure failed once on `no such column: attorney_slug`, fixed, 5 successes since, firm columns filled (pw 580,589/634,638; lca 326,385/437,496); processing-times 8/8 green, as_of 2026-08-31, change gate works, 15/15 cutoff cells agree with permtrack; probe-state-dept still-closed reading valid with controls; IndexNow submitted 13,761 URLs in two batches; Semgrep/CodeQL weekly green; all six secrets exist; GITHUB_TOKEN default read; claude workflows skip correctly; no `pull_request_target`.

**Ingest scripts.** `lib_gov_data` full header set, 403/429/503 retries, links discovered not constructed, cells resolved by `r=`; `lib_turso` transient-only retries; flag loader `ensure_columns` before indexes, backfill before the sha check, batched UPDATEs, WRITE_PAUSE_S; file discovery picks Q4 over Q3 and FY2027 Q1; decision_date is the latest of four dates; the Oct 2025 hole is real (21 PERM, 9 PWD decisions); processing-times `.mts` idempotent on DOL's as-of, anchored regex confirmed; visa bulletin SOURCE_RANK clause order correct, 84 months, 0 mirror rows; perm disclosure resolves columns by name with the core guard, payload guards in the workflow; `turso_migrate --incremental` wrote 21,748 of 373,939 rows; entity slug rules mirror the TS; live-recent membership rule, two-sided normaliser, changed-slugs cap 800 = MAX_PATHS 800; identity rebuild refuses to 404 a live slug; all timestamps UTC.

**Turso read layer and API routes.** 20 s deadline with one retry through the cause chain, writes only via `exec()` (four sites, all `INSERT OR IGNORE` except the budget upsert); every daily perm_docs read has an 8-day cutoff and all docs are fresh today; prefix guards on all three lookups with normalisation; revalidate endpoints secret-gated, fixed lists, drift tests walk the app tree, slug regex and 800 cap; every `/api/*` route validates lengths/regexes, clamps LIMITs, sets CDN headers; `/api/decided-cases` refuses with 400 not an empty 200; `coverageFor` reports the 2026-07-01 to 08-25 gap; 93 indexes confirmed, every SQL literal mapped to one except the wage family (M13) and the stale-doc fallbacks (M12); `flagMerge` live-wins/file-supplies-wage; robots disallows `/api` and `/perm-case-status?`.

**Convex.** 13 crons registered, every target `internal.*`, every one measured firing on Sep 5 and 6 at the documented EDT times; 0 errors in 980 success lines, 0 systemErrors in 7 days; all three subscribe mutations check the global budget before the row write; `isPlausibleEmail` length-before-regex in all five copies; every public route calls an internalMutation, GET renders a POST button, CORS allowlist, 400 vs 429 mapped, HTML escaped; token purposes at every site, replayed confirm can't undo an unsubscribe; `sendEmailWithRetry` is the only send path besides OTP/reset (both check `{ error }` and the blocklist); Resend 7-day 81/81 delivered; all 23 subscribed case numbers exist; sweeps walk `by_alert_sweep`, claim budget before render, reschedule on progress; the one `.extend()` site carries the exactness assertion; every `: undefined` patch is deliberate; account deletion cascade complete (14 tables, calendar, Resend contact); DMARC `p=reject`, DKIM/SPF live.

**Authenticated app.** Every public function without a direct auth call is a public reader, uses `verifyOwnership`, `requireProfile`, identity-derived email, or runs through an auth-checked query; `requireAdmin` re-reads the user row each call; no firm feature exists (hierarchy removed Aug 24, 0 rows); cases hard-deleted with audit + cleanup; canonical composites used everywhere (no `addDays(…,365|180|30)` outside lib/perm); `calculatePWDExpiration` OEWS rule with 21 boundary tests; `calculateFilingWindow` PWD cap tested; cascade 25 tests; business days skip federal holidays incl. Juneteenth; per-user timezone for "today"; OTP is one text input; push subscriptions validated; digests one per user, verified-gated; calendar tokens AES-256-GCM, invalid_grant auto-disconnect; chat route BotID + IP limit + auth + 10-step cap + 503 on all-provider failure; export owner-scoped; 30-day deletion grace with cancel and safety-net cron; concurrent edits are field-level last-writer-wins on one owner's record.

**Served site.** 30/30 URLs 200; 7 security headers on all; one h1 and self-canonical on all; JSON-LD parses on all, Dataset creator is an `@id` reference; 29/30 descriptions 70 to 155; no empty-state copy on any page; homepage band date == data_freshness == processing_times == llms.txt; junk URLs 404 with noindex, entity 404 cached; http → https 308, www → apex 308; `audit_all_pages` 0 findings over 61 URLs/39 templates; `audit_ssr_visibility` 182 clean with control alive; `audit_glued_text` 0 pairs with control; served CSS has 0 `cursor:none`, 0 tinted shadows on public pages, global reduced-motion rule; single deployment confirmed by Age arithmetic.

**External state.** Production deployment READY on a 4-core Standard machine with cache restored; env names present (REVALIDATE_SECRET, TURSO_RW_AUTH_TOKEN, etc.); no 5xx in 24h except one 502 on the PostHog proxy; TLS to Oct 29 2026 (auto-renews); DNS A/CNAME/NS/SPF/verification correct; domain expires Nov 25 2026, transfer-locked; PostHog capture live (30d 2,845 visitors; referrers t.co, google, chatgpt.com); GitHub secret scanning + push protection on, 0 alerts; branch protection with two required checks; gh auth on the persona; Gmail read-only path works.

**Secrets and compliance.** No secret in 1,766 tracked files or history; `.gitignore` covers `.env*`, `.vercel`, `.convex`, scratchpad; Convex prod Turso token decodes to `ro`, web writes only via `exec()` on the RW token; retention claims match code (90d notifications, 90d conversations, 24h rate limits, 30d grace); GPC honoured for PostHog; PostHog proxied via `/ingest`; Google Fonts self-hosted; outage behaviour sound for Turso (deadline, `.catch(() => null)` at 24 sites, error boundaries), Convex (middleware returns before touching it on public routes), Resend (state advances only on success), DOL (backoff, clean stop), AI (503).

**Local checks.** `pnpm typecheck` 0 in 27 s; convex 1,116 / ssr 11 / app tests 126 / turso 280 pass; 10 of 11 Python test scripts pass (the 11th is H5); every one of 335 vitest files owned by exactly one project; no test fetches a real URL; no `.only`; `vi.mock` factories use `*Icon` keys.

**Front-end.** 0 custom cursors, 0 non-Phosphor icon libraries, 0 `<path transform=`, the two inlined arrows match Phosphor byte-for-byte, 0 thin weights, 0 `h-screen`, grid-track gate holds, header height variables used 49 times, alt on all 20 images, MDX has 0 em-dashes/emoji, article body 17px, CSS reduced-motion universal, tap targets 44px on mobile, muted-foreground 5.50:1 light / 7.57:1 dark, trust badges factual, two-doors IA holds.

---

## 5. Could not check

| What | Why | Manual step |
|---|---|---|
| Which Turso plan is in force, this cycle's rows read/written, per-table storage | No CLI or platform token; repo notes disagree (10M writes = Free; 2.5B reads = Developer) | app.turso.tech → Usage and Billing; note plan, PITR window, writes this cycle. M5's verdict flips between "blocker" and "cost" on this |
| Whether DOL's endpoint exact-matches an unpadded serial, and answers a mixed-prefix batch | DOL calls forbidden this pass | From the laptop: POST `["G-100-26163-008061"]` then `["G-100-26163-8061"]`, and `["G-100-26246-214078","G-200-26246-214078"]`; compare returned strings |
| The counter's current position (assumed ~218,500) | Extrapolated from three (serial, time) points | Before the recovery run, probe 25 serials at 218,000 / 220,000 / 222,000 under G-100/G-200 for codes 26247 to 26249 |
| Why backfill day 26161 burned the cap with 0 rows (root cause of C2 is the wrap; this confirms it) | Needs the run log or a dispatched dry run | Open the pwd-status-direct run that ended Sep 3 4:18 AM EDT and read the "26161: serials lo-hi" line |
| Whether one serial can be held by two prefixes | Single 50-serial sample underlies the design | POST 50 known P-100-26239 numbers re-prefixed I-200-26239; expect `[]` |
| Who is behind the `/perm-case-status?case=P-` traffic and the 8 to 10 req/s | `vercel logs --json` carries no user agent | Vercel → Observability → Requests grouped by User Agent and Path, last 24h; then Firewall rule |
| Exact Vercel invocation and CPU counts, and whether Node middleware bills as invocations on Hobby | `vercel usage` prints dollars only | Vercel → Usage → Functions for Sep 1 to 6; read docs/limits Hobby row for middleware |
| Observability Plus toggle | CLI exposes usage, not billing settings | Team Settings → Billing → Observability Plus → Manage projects |
| Sentry alert rules and 14-day issues | No MCP; local token is release-scoped (403) | sentry.io → perm-tracker → Alerts; Settings → Notifications |
| Domain auto-renew and payment method | RDAP does not expose it | account.squarespace.com → Domains → permtracker.app → Auto-renew ON before Nov 25 |
| Convex plan, backup setting, permCases doc count, bulletinAlerts rows, row counts above 1,000, uncaught-cron notification | Convex MCP timed out; `npx convex data` capped at 1,000 and printed nothing for bulletinAlerts | dashboard.convex.dev → giant-dragon-464 → Usage, Data, Settings → Backup, Integrations |
| Which Resend key Convex's AUTH_RESEND_KEY is (before deleting the other three) | API returns names only | Compare key prefix in the Resend dashboard with `npx convex env get AUTH_RESEND_KEY --prod | cut -c1-6`; then send one test email |
| Scope of the Vercel TURSO_AUTH_TOKEN in Preview/Development and the GitHub Actions copy | Values redacted/write-only | Reveal in the dashboard, decode the JWT `a` claim; expect `ro` on all three Vercel copies |
| Whether `/perm-rfi-audit` renders the 2-case DETERMINATION ISSUED stage | No browser this pass | Open the page and look at the small-stage list |
| Whether Monday's pending run actually masks row 57 | Future event decided by GitHub's queue | Mon Sep 7 after ~10 AM EDT: run `python3 scripts/check_ingest_health.py` locally and compare with the scheduled run |
| Whether the quarterly workflow rebuilds `perm_entities` and `daily_decisions` (decides if M8 alarms are false or true) | Tracing steps 212 to 300 was another surface's work | Read `perm-disclosure-ingest.yml:212-300` and grep the named scripts for writes |
| Full vitest suite (111 unit + 130 remaining component files), e2e spec, order dependence under CI shuffle | `pnpm test:run` forbidden this pass | After Sep 7: `pnpm test:run` once; `CI=1 pnpm exec vitest run --project components --sequence.seed=8` and 13 |
| Live headers of `/api/perm-wages` | One request costs ~1.5M billed rows | `curl -sI` once after the baseline |
| Rendered legibility of the 10 to 11px labels, CLS from the hero image, Motion ignoring reduced motion | No browser, no Lighthouse this pass | One chrome-devtools instance at its 500px floor on `/for-attorneys` and a case page; Speed Insights CLS after the next deploy |
| The uncovered-gap panel on `/perm-decision-activity` for Aug 10 | `?date=` is inert on a static page; the panel renders after hydration | Pick 2026-08-10 in the day picker; `curl /api/decided-cases?date=2026-08-10` expecting a 400 |
| Whether GitHub's red-run emails reached the owner | Inbox settings unreadable | Search the persona inbox for "Run failed: PWD case status" dated Sun Sep 6 12:38 PM EDT |
| Direction of the 87 overwritten `pwd_cases` rows | Needs both workbooks | `--dry-run --dump-rows` against FY2025 and FY2026_Q3 for one of the 23 case numbers |
| Whether DOL has posted a FY2026 Q4 or FY2027 Q1 file | No dol.gov call | curl the performance page from a residential IP, grep `_FY2026_Q4|_FY2027` |
| `docs/API.md` function-by-function | 3,550 lines organised by file; regex validated nothing | Extract per-file names and diff against `export const` in `convex/` |

---

## 6. Edge cases

**Examined:** year boundary in day codes (25365 → 26001, pinned); the counter not resetting at New Year (26001 started at 519,829); leap day (366 only in 2024); DST (no local arithmetic in ingests; the one client-side DST bug is M22); day codes 000 and >366 absent; serials all 6 digits in storage; no future filing or decision dates; the Oct 2025 shutdown hole is real and unfabricated; XLSX omitted cells resolved by reference; sha matches but columns changed (`ensure_columns`); a file that removes rows (never deleted, per-file count would show it); duplicate case numbers across programs (separate tables); fiscal-year boundary and `--fy` history loads; 404 vs 403 (never constructed paths); live/published slug collision (measured zero); a user deleted mid-sweep (email-keyed alerts, re-checked before send); a case with every date null; a user with 0 cases; concurrent edits (OCC, single owner); a case absent from the mirror or vanishing mid-sweep; DOL over-return; three consecutive DOL failures; Resend 429 vs quota error; cron double-fire around a deploy; the exactness assertion on the one `.extend()` site; doc exactly at the 8-day cutoff; lowercase/whitespace case numbers; the legacy `A-` form in the app form; the entity-page hydration control; junk and case-sensitivity URL variants; HEAD vs GET; trailing slashes.

**Not yet covered:** the serial wrap inside a single probe (C1/C2 fixes carry tests, none exist today); a DOL maintenance window answering 200 with an empty body (reads as "no filings"); a mixed-prefix batch to DOL; a serial claimed by two prefixes; a filing day with zero PERM filings inside a run of PWD/LCA serials (the current dry streak treats it as the end); a Convex cron throwing before its try block (notification path unconfirmed); a Resend suppressed recipient (send succeeds, nothing delivered); a hard bounce on an attorney's mailbox; a quiet-hours user on the daily reminder; an import of 10k rows; a suspended user with an existing session; prompt injection via web search in AUTO mode; an entity name in non-Latin script (empty slug); a wage of 11,111,111 in a wage-descending sort; a stale `perm_docs` doc past 8 days on the dynamic lookup path; the ISR cache after a deploy under crawler load; a preview branch holding an RW Turso token; a GPC visitor inside the app (replay suppression not executed); a DST-crossing recruitment span in the form (M22, now measured, fix untested); the health check running INSIDE a sweep (observed Sep 1, effect unmeasured).

---

## 7. Money

| Bill or cap | Current position (measured) | Trend | When it matters |
|---|---|---|---|
| Vercel Pro, $20 credit, cycle Aug 28 to Sep 28 | Sep 1 to 6: **$40.25**: ISR Writes $18.06, Fluid CPU $8.77, Origin Transfer $4.60, Pro $3.23, Invocations $1.94, Memory $1.73, Observability Events $1.32, Build CPU $0.10 | ISR is deploy-count-shaped (5 deploys on Sep 1 cost $3.23 in one day; every deploy cold-starts the cache); CPU and invocations are crawler + proxy shaped (H1, H11) | Already past the credit. The freeze stops ISR; only the WAF rule and the proxy matcher move CPU/invocations |
| Vercel Hobby (the target) | 1M invocations, 4 CPU-hours, 360 GB-h memory per month; non-commercial only | Derived ~16M invocations and ~340 CPU-h per month at the sampled rate [inferred from $ at iad1 rates, medium confidence] | Not reachable by cutting entity pages (2.6 percent of requests). Reachable only if the crawler is stopped and the proxy narrowed; the terms clause is a separate risk |
| Observability Plus | $1.32 / 6 days ≈ $6.60/month | Scales with request count | Turn off in the dashboard (M24) |
| Turso | 1,501.9 MB; plan unknown; reads fixed Sep 2 (was 7.75B on one invoice, 11.58B in two days) | Writes: ~3M/month baseline + ~2.2M crawler counter + one ~8M FLAG month; storage +~1.2 GB/yr [inferred] | Q4 FLAG file month (Oct/Nov) breaks Free's 10M writes unless M5 ships; Free 5 GB ~2029 |
| Turso PITR | 24h on Free, 10d Developer, 30d Scaler | n/a | Any bad `INSERT OR REPLACE` run older than the window is unrecoverable (M27) |
| DOL discovery budget | 2,000 DOL requests/day for P-/I-, spent by bots by ~9 AM UTC; PERM 1 to 5/day | 74,220 attempts on Sep 4 | Every day until the WAF rule (H1) |
| Resend, 100/day shared | 15-day: 169 sent, 169 delivered, 0 bounced; peak 32/day (Mondays, weekly digests) | Ledger claims 75 list + 25 auth; uncounted paths could push a bad Monday past 100 (M14) | A signup burst or contact-form flood on a Monday |
| Convex free tier | 1M calls, 0.5 GB storage, 1 GB I/O; permCases holds 373,939 docs nobody reads; tripped once before | Rewritten wholesale each disclosure run | Sep 20 cron unless H7 ships |
| GitHub Actions | Public repo, free; Semgrep 22 min per push (15 pushes in 2 days); Sunday sweep 170 min of runner time for nothing | n/a | No cost; time and signal only |
| Domain | Expires Nov 25 2026 (80 days), Squarespace, transfer-locked | Auto-renew unverified | Confirm before Nov 25 |
| TLS | Let's Encrypt to Oct 29 2026 8:43 AM UTC (4:43 AM EDT), Vercel auto-renews | n/a | none |
| Dependabot preview builds | ~6 preview builds per Monday, 2 fail on the AI SDK peer gap | Weekly | Mon Sep 7 5:00 AM EDT unless M25 ships tonight |

---

## 8. The fix plan

Rule: nothing under `v2/src`, `v2/content`, `v2/convex`, `v2/public` (or `package.json`) changes without a separate deploy decision, because each such change is a production build and a cold ISR cache. Everything in group A is scripts, workflows, docs, manual DB writes, or dashboards, and ships without a build (the ignore script skips those paths; probe it in both directions after any change to it).

### Group A: no deploy

| # | Step | Touches | Depends on | Verify by |
|---|---|---|---|---|
| A0 | **Tonight, before 5 AM Monday:** move Dependabot to Tuesday and skip `dependabot/*` refs in the ignore script (M25). Owner decision on the WAF rule (H1): add it tonight and Monday measures the product, or hold it and Monday measures the crawler. | `.github/dependabot.yml`, `v2/scripts/vercel-ignore.sh`, Vercel Firewall | none | No preview deployments Monday; `pwd_discovery_budget_2026-09-08` under ~500 if the rule is on |
| A1 | Tue Sep 8: fix `test_flag_disclosure.py` fixtures (with a firm column), split the three scripts into named steps, wire `test_entity_identity.py`, swap the README badge (H5) | `v2/scripts/test_flag_disclosure.py`, `.github/workflows/test.yml`, `README.md` | none | Tests run green with "Run full test suite" executed, ~335 files |
| A2 | PERM discovery rewrite: serial-major walk carrying the day code, G-100 + G-200, `:06d`, wrap, persisted frontier, cap 400 with "partial" (C1, M2); then the laptop recovery run | `v2/scripts/ingest_case_status_direct.py`, `test_case_status_direct.py`, `v2/CLAUDE.md` | A1 (so CI can run the new tests) | New tests red on revert; log "N new cases recorded", N > 0; `MAX(case_number)` past 26246 |
| A3 | PWD/LCA prober: cluster-split `day_windows`, within-day resumable progress, frontier-anchored daily discovery, "partial" on empty windows, chain step `!cancelled()`, guard order, discover timeout 90m (C2); then one backfill dispatch from 2026-06-10 to today | `v2/scripts/ingest_pwd_status_direct.py`, `test_pwd_status.py`, `.github/workflows/pwd-status-direct.yml` | A2 (share the walk if possible), A5 (drop dup indexes first) | Fixture test red on revert; `byMonth` gains 2026-07; `lastDayDone` >= 26251 |
| A4 | Health: frontier check, discovery yield rows, no stamp on zero work, `as_of` from newest filing, "partial" on truncation, mode keying + `--$MODE` in hooks, per-pass PWD/LCA keys, hook read-back with `::error::`, `except BaseException`, `mismatch` broken, `workflow_run` triggers, i485 hook + retries + laptop capture (C3, H2 health half, H8, M7, M8, M10) | `check_ingest_health.py`, `test_ingest_health.py`, `record_ingest_failure.py`, `lib_turso.py`, both ingests, `ingest-health.yml`, `case-status-direct.yml`, `pwd-status-direct.yml`, `i485-inventory-ingest.yml`, `ingest_i485_inventory.py` | A2, A3 | Checker exits 1 today naming 9/9/10-day frontiers; fixture with later `--pending ok` still red; resilience fixture prints `::error::`; `ingest_runs` gains an i485 row |
| A5 | Manual Turso writes, after Monday: delete the 2 P-100 rows from both tables, drop the 5 duplicate indexes, delete the `uscis-i140-times` freshness row, one-off `is_final` UPDATE for M9's rows once B1 ships; add the prefix invariant and the fixture exclusion to the ingest (M1, M3, M4, M8) | Turso (RW token from the laptop), `ingest_case_status_direct.py` | none | Invariant query 0; EXPLAIN fresh-request still SEARCH on the surviving index; both pending totals equal |
| A6 | Sunday sweep restructure: PWD 180-day window, LCA daily 1/7 slice (or 1/28), SIGTERM handler; interim workflow-only `--full --program pwd` on Sunday (H2) | `ingest_pwd_status_direct.py`, `pwd-status-direct.yml`, `test_pwd_status.py` | A4 | `sweep_runs` lca full/slice row complete=1 inside the timeout; Sunday writes fall from ~300k to ~50k |
| A7 | Before Sep 20: gate the disclosure workflow on `contentHash`, remove the Convex store steps, make the scheduled loader own identity and write aliases, upsert-then-prune in `build_entity_detail`, stamp `entities`/`daily-decisions`, delete `backfill_permtrack.py` and `rebuild_entities.py`, fix `PAGE_FLOOR`, drop or condition the 20th cron; rerun the identity rebuild once (H6, H7, M6, M8) | `perm-disclosure-ingest.yml`, `turso_migrate_public.py`, `turso_migrate.py`, `rebuild_entity_identity.py`, `build_entity_detail.py`, `store_entities.py`, `store_cases.py` | A5 | Dangling-alias LEFT JOIN = 0 after a dispatched run; no "Added N documents to permCases" in the log; Convex Usage falls |
| A8 | Before Oct 10: FLAG loader diff by fingerprint, newest-decision-wins conflict rule, core-column guard, `changed=` output gating the expiry step (M5, M28, S8, WF-8) | `ingest_flag_disclosure.py`, `flag-disclosure-ingest.yml`, `v2/CLAUDE.md` | none | Dry run against an unchanged file logs ~0 writes; Oct 10 log does not say "reloading" |
| A9 | Backups and credentials: weekly observation dump job, `npx convex export --prod` now, confirm Convex backup setting, delete the three dead Vercel env vars and two Resend keys after matching the Convex key, write the Turso rotation runbook (M27, SEC-7, SEC-8) | `.github/workflows/backup-observations.yml`, `scripts/dump_observations.py`, dashboards, `v2/CLAUDE.md` | none | The artifact restores locally with matching counts; one test email after key deletion |
| A10 | Dashboards: Observability Plus off, Sentry alert rule + email, Resend webhook events, Vercel Usage counts read, Squarespace auto-renew, PostHog recordBody decision, Firewall UA read (M24, M26, M16 half, EXT-3, EXT-11) | none in repo | none | `vercel usage` Observability $0; a sentry-check event lands in the inbox; Resend webhook lists bounce events |
| A11 | Docs and hygiene: reCAPTCHA banners, five projects, email arithmetic, glued-text paragraph, cron labels in UTC+EDT/EST, guides count, sitemap path, memory corrections (Convex tables, storage, replay masking, Speed Insights), `nonlocal sent`, pyflakes imports, action timeouts, `env:` for `${{ inputs.mode }}`, visa-bulletin provenance constant, cross_check count, ignore-script pattern, WF-11 note, TEST_README phantom scripts (DOC-3 to DOC-9, LC-4, LC-6, LC-2 README half, S10, S13, WF-9, WF-10, WF-11, SEC-9 workflow half) | `CLAUDE.md`, `v2/CLAUDE.md`, `v2/TEST_README.md`, `.planning/*`, workflows, scripts, memory files | none | `python3 scripts/audit_articles.py`-style greps for the corrected strings; probe the ignore script both directions |

### Group B: needs a deploy decision

Batch these into as few builds as possible; each build cold-starts ISR (~$3 on a 5-deploy day measured).

| # | Step | Touches | Depends on | Verify by |
|---|---|---|---|---|
| B1 | **First post-baseline build** (correctness on the flagship page and honesty copy): H3 last_checked_at, H4 rfi-audit paragraph, H1 code half (check-then-increment, skip discover when the file holds a final, log once), M12 stale-doc empty state, M11 `/signup` force-static, H10 pulse-dot and `text-primary-text`, FE-8 skeleton, H9 privacy sections, D6 guide, M9 status vocabulary (Python + TS same commit), LIVE-5 description, FE-9 welcome subject | `src/lib/turso/{caseLookup,rfi,caseDiscovery,flagCases,backlog,publicData}.ts`, `src/lib/casePosition.ts`, `src/components/tools/{CaseStatusResult,PwdStatusResult,LcaStatusResult}.tsx`, `perm-rfi-audit/page.tsx`, `signup/page.tsx`, `for-attorneys/page.tsx`, `globals.css`, `privacy/page.tsx`, `layout.tsx` (Ahrefs gate), `content/guides/perm-case-number-format.mdx`, `src/lib/turso/pwdCases.ts`, `src/lib/flagCaseNumber.ts`, `tools/i140-trends/page.tsx`, `convex/welcomeEmail.ts` | A0 WAF decision, A5 | Component test for the check date; curl the case page and privacy page; `pnpm test:run` green; built-CSS `infinite` gate |
| B2 | **Second build** (cost and security): H11 proxy matcher (after a 10-minute log sample), M13 wages cache header + one-pass query, M14/M15/M16 email caps, dedupe and bounce handling, M17 `checkAuthRateLimit` internal, M18 server-side suspension, M20 import cap, M22 DST fix, SEC-6 refund, EXT-10 browserslist override, CX-5/CX-6 | `src/proxy.ts`, `api/perm-wages/route.ts`, `convex/{caseAlerts,contactForm,supportEmail,reengagement,notificationActions,http,schema,authRateLimit,abuseDetection,lib/auth,cases,rateLimitConfig}.ts`, `src/lib/forms/case-form-schema.ts`, `package.json` | B1 | Middleware share < 20 percent in a fresh log sample; convex-test cases for dedupe, cap, suspension; TZ-pinned schema test; DEV reproduction of M17 before/after |
| B3 | **Third build** (craft floor and growth): M23 label floors and running-prose size, MotionConfig, app-side pulses, hero image dimensions, dash placeholders, FE-10 to FE-18, M19 reminder fan-out and pagination, M21 chat injection boundary and quota, AUTH-7/9/11/12, T7 to T10, CX-4/7 to 10, LIVE-7/9, LC-5, SEC-10, FE-16 data-driven badges | broad `src/components/**`, `src/app/**`, `convex/**` | B1, B2 | Static gates listed in M23; Speed Insights CLS on `/for-attorneys`; Insights tab clean after a seeded read test |

Where "best" differs from what was asked, in one place: the owner asked for a free or ~$20 bill via entity-page cuts and a frozen Monday. The measurements say the bill is the crawler, the proxy and the deploy count, not the page count; that a WAF rule tonight changes Monday's baseline but makes it measure the product; and that the two critical data defects (C1, C2) are scripts-only and can be repaired Tuesday from the laptop without touching Vercel at all.


---

# Appendix: completeness critic (what the sweep may still have missed)

- **[high] A0/H1 'enable Bot Protection' would silently kill the three nightly revalidation POSTs and the owner's own curl/urllib audit scripts, and a 'challenge non-browser UA on /perm-case-status?case=' rule challenges the user-initiated AI fetchers the owner is courting.**
  Why: MEASURED from vercel.com/docs/bot-management (last updated 2026-08-11): the managed ruleset 'challenge[s] non-browser traffic', 'prevents requests that falsely claim to be from a browser such as a curl request', exempts only verified bots (list includes chatgpt-user, claude-user, oai-searchbot, perplexitybot, ahrefssiteaudit), and says trusted automation needs a separate bypass custom rule. MEASURED in the workflows: case-status-direct.yml:139-142, processing-times-ingest.yml:112, flag-disclosure-ingest.yml:150 all run `curl -fsS ... || echo "revalidation call failed"`, so a challenge prints one line and the run stays green; nothing in the fix plan adds the bypass. Verified-bot exemption applies to the managed ruleset, not to a custom challenge rule, so ChatGPT-User/Claude-User fetches of a pasted lookup URL (chatgpt.com is a measured referrer) get challenged; robots.txt does not govern user-initiated fetchers. Also MEASURED from the rate-limiting doc: rate limiting IS on Hobby (1 rule per project, 3 custom rules total, 1,000,000 allowed requests included) and is usage-based on Pro; the synthesis says 'no build' but not 'metered', and the Hobby target's whole design must fit one rate-limit rule.
  Method: Before enabling anything: add a bypass custom rule for path prefix /api/revalidate- (or header x-revalidate-secret present) and for the Actions runner, key the /perm-case-status rate limit on IP only with action 429 (not challenge), leave the managed ruleset in Log mode for 24h and read the Firewall tab; verify the next scheduled runs still log {"revalidated":N} and that `python3 scripts/audit_all_pages.py --per-shape 1` still gets 200s.
- **[high] B1 and B2 change convex/ (welcomeEmail.ts in B1; schema.ts plus 12 convex files in B2) but the fix plan never names the Convex deploy, its ordering, or the committed convex/_generated requirement.**
  Why: Root CLAUDE.md: Convex deploy is manual (`npx convex deploy -y` from v2/), Vercel builds from git and needs convex/_generated committed after codegen or the build typechecks against yesterday's API; v2/CLAUDE.md records a production outage from deploy skew (a required prop fed by a Convex query undefined until the functions deployed). B2 adds an index (M15) and changes checkAuthRateLimit to internalMutation (M17): deploying Vercel first breaks login rate-limit calls against a deployment that still exposes the old shape, deploying Convex first breaks the old frontend's public call. Neither order is stated, and 'triggers_vercel_build: true' is the only side effect flagged.
  Method: Add an explicit step to B1/B2: deploy Convex first with backward-compatible shapes (keep the public checkAuthRateLimit as a thin wrapper for one release), commit _generated, then push; verify skew by loading /login on the live site between the two deploys.
- **[high] The money table's 'Convex free tier' line is probably wrong: the deployment was disabled for exceeding the free plan on Aug 25 and has been importing 455k documents and firing 13 crons since Aug 30, which on Convex's Starter plan does not happen inside the same billing month.**
  Why: MEASURED: .planning/SESSION-STATE-2026-08-25.md:7 'Convex is STILL disabled (reads and writes both; permCases:getMeta returns "You have exceeded the free plan limits")' and :34 lists 'Convex Pro, $25/mo' as option 1; the Aug 30 disclosure run then wrote 373,939 permCases (S2) and the Sep 5-6 success log shows 0 failures (CX). INFERRED: an upgrade to Pro ($25/mo plus usage) happened between Aug 25 and Aug 30, so §7 is missing a ~$25/mo line and H7's 'free tier limits' framing (and its Sep 20 risk) is unverified either way.
  Method: dashboard.convex.dev -> team -> Billing (already listed as manual); until read, carry '$25/mo probable' in §7 rather than 'free tier', and note that on Pro the 455k-doc import is a usage bill, not an outage risk.
- **[medium] Turso storage horizon '~1.2 GB/yr, Free 5 GB ~mid-2029' is inferred from an assumed 0.5 KB/row; the synthesis's own measured delta implies ~0.73 KB/row and a 2028 horizon.**
  Why: MEASURED today: PRAGMA page_count 384,730 x 4,096 = 1,502.9 MB, freelist_count 0 (no reclaimable pages, so the figure is real). The synthesis attributes the +1,081 MB from Aug 27 to Sep 6 to the FLAG tables (~1.48M rows: pwd_cases 634,638 + lca_cases 437,496 + lca_case_status 309,401 + pwd_case_status 96,009), i.e. ~0.73 KB/row all-in with their 9 indexes. INFERRED from §3's own per-table rates (2.3 to 2.6M rows/yr summed): 1.7 to 1.9 GB/yr, Free 5 GB in ~1.9 to 2.1 years (mid/late 2028), Developer 9 GB ~2030. INSERT OR REPLACE on the Q4 files will also churn pages; freelist is the number to re-read after Oct 10.
  Method: Read `PRAGMA page_count` and `PRAGMA freelist_count` (two 1-row reads) after the Oct FLAG load and monthly; write them into perm_docs from the ingest so the rate is measured, not assumed.
- **[medium] The 'one serial counter, no serial held by two prefixes' premise behind C1 and C2 is checkable read-only from DOL's own disclosure files and it holds; two could_not_check rows can be closed. The wrap-window line the synthesis wanted from the run log is also readable.**
  Why: MEASURED via PK-range reads on perm_cases/pwd_cases/lca_cases (independent of the prober, which excludes known G- serials via known_serials at ingest_pwd_status_direct.py:247-259 and so can never observe an overlap): day 25100: G-100 211, G-200 48, P-100 795, I-200 45 serials, 0 overlaps in every pair; day 26100: I-200 3,052, I-203 34, P-100 25, G-100 4, G-200 4, 0 overlaps; live tables on 26238/26239 also 0 (587/745 P, 1,649/865 I-200, 315/291 G-100). Day 25300 (Mon Oct 27 2025) holds 0 rows under all five prefixes, the shutdown. MEASURED via `gh run view 33729300228 --log`: '26161: serials 1-999,997, 213 already known' at 3:41 AM EDT Sep 3, 'request cap 4500 reached inside 26161 (I-200-)' at 4:17 AM EDT. Still open and NOT sequenced in A2: whether DOL answers a mixed G-100/G-200 batch, which the C1 design (25 serials x 2 prefixes) depends on.
  Method: Repeat the intersection over 5 more day codes with the same script (10 s, ~10k rows read) and cite it under checked_ok; put the mixed-prefix DOL probe as A2's first step, before the rewrite.
- **[medium] The C3 frontier alarm (FRONTIER_MAX_DAYS=5, 'fail when the 3-day weekday sum of discoveries is 0') has no shutdown or DOL-outage mode and will page every day of one; FY2027 starts Oct 1, 2026, 25 days out, and the corpus already holds a shutdown month.**
  Why: MEASURED: day 25300 has zero filings across PERM, PWD and LCA; perm_cases received_date 2025-10 = 21 rows against ~450/month either side (D checked_ok). The synthesis's (e) 'log DOL answered nothing for N requests' is a log line, not a suppression, and the plan has no mute key; every run of a multi-week shutdown would exit 1 and, per the ingest-health design, keep alarming with nothing to fix.
  Method: Make the frontier check read the prober's own audit row: 'asked N, DOL returned []' for >= 2 consecutive weekdays with the endpoint answering 200 suppresses the frontier alarm and emits a distinct 'DOL quiet' line; add a perm_docs mute key with an expiry and a test for both.
- **[medium] Mon Sep 7 is Labor Day, and the synthesis treats it as 'the clean baseline day' without saying what that day can and cannot measure.**
  Why: No DOL filings on a federal holiday, so PERM discovery reporting '0 new cases' on Monday is expected and cannot distinguish C1 from the holiday; human traffic is atypical, while crawler traffic and the Monday 11:00 AM EDT weekly digest (measured Resend peaks 32-34/day on Mondays) are normal. Monday is a valid cost/crawler baseline and an invalid product-traffic baseline; the P5 masking prediction also runs on a day with no filings to discover.
  Method: Name Tue Sep 8 as the traffic baseline and keep Monday for the ISR/CPU baseline; compare Monday's PostHog pageviews against the prior four Mondays before drawing any traffic conclusion.
- **[medium] Contradiction: EXT-11 says PostHog replay records 'with input masking off'; SEC-2 and §1 say it is masked and app-only.**
  Why: MEASURED in src/instrumentation-client.ts:82-83: `disable_session_recording: true` and `session_recording: { maskAllInputs: true, maskTextSelector: "*" }`, GPC read at :52-53; no recordBody or recordHeaders option in the file, so EXT-11's 'captures request/response bodies and headers' can only be a PostHog project setting read from the dashboard (inferred, unlabeled). Two readers, opposite claims, both carried into the report.
  Method: Keep the code reading; move EXT-11's body/header claim to could_not_check unless the PostHog project settings page was actually read (state which).
- **[medium] The 'no 5xx in 24h except one 502' checked_ok cannot have been observed the way it is described, and `vercel logs --json` genuinely carries no user agent.**
  Why: MEASURED: `vercel logs --json` rows carry exactly these keys: branch, cache, deploymentId, domain, environment, id, level, logs, message, projectId, requestMethod, requestPath, responseStatusCode, source, timestamp, traceId. The same reader says the 100-row `--level error` cap was 100 percent the budget-refusal line, so a 5xx could not appear in that query at all; the 502 must have come from another query that is not named.
  Method: Stream `vercel logs --json` for 10 minutes and count rows with responseStatusCode >= 500 (the field exists), and state the query; for attribution use the Firewall tab, or add one server-side `headers().get('user-agent')` to the refusal log line in the next build so the CLI logs carry it.
- **[medium] No reader covered accessibility semantics: keyboard operability, labels, live regions, focus order on the two doors' forms.**
  Why: FE covered contrast, tap targets and alt text only. MEASURED (crude, single-line grep): 8 files use aria-live; a skip link to #main-content exists (2 refs); `<html lang="en">` at layout.tsx:171; 5 div/span onClick handlers in src/components; 29 `<input` lines in src/components/tools with no id or aria-label on the same line (multi-line JSX, so unverified; a `<label htmlFor>` partner may exist). The owner's craft floor names full keyboard operability and no hover-only paths; nothing measured it.
  Method: After the baseline: one chrome-devtools instance running axe-core on /, /perm-case-status?case=G-100-26240-200246, /signup and one entity page; statically, pair each of the 29 inputs with a label via a small AST pass over src/components/tools.
- **[medium] Money and caps table omits every non-Vercel/Turso/Resend quota the product can hit: AI providers, web search, PostHog, Sentry, Google OAuth publishing status.**
  Why: The chat route degrades to 503 when 'all providers fail' (checked_ok), but the rate limits behind that (Gemini/Groq/Mistral/OpenRouter/Cerebras per-minute and per-day free-tier caps) are the real cap and appear nowhere; Tavily/Brave keys are read at convex/webSearch.ts:92 and :142 with no quota line; PostHog (events, replay, error tracking) and Sentry quotas are absent; a Google OAuth app left in Testing status expires refresh tokens after 7 days and would present exactly as the implemented invalid_grant auto-disconnect. MEASURED read-only via `npx convex data systemErrors --prod --limit 100` (the CLI works while the MCP times out): 0 invalid_grant rows; 28 'Google Calendar API error: 410' rows, all stamped Thu Mar 5 2026, so no current evidence, but the axis was never examined.
  Method: One line per provider with its dashboard: providers.ts model list -> each vendor's free-tier RPM/RPD; PostHog project usage page; Sentry quota page; GCP OAuth consent screen 'Publishing status'. All read-only.
- **[low] Uncovered subsystem: the service worker / PWA layer (Serwist), which is production-only and re-downloads its whole precache on every deploy.**
  Why: MEASURED: src/app/sw.ts precaches `self.__SW_MANIFEST` with skipWaiting and clientsClaim, NetworkOnly for document/script/style, CacheFirst same-origin images (100 entries, 30 d) and fonts (1 yr), and carries the push handlers; `disable: NODE_ENV === 'development'` (next.config.ts:19) so `pnpm dev` never exercises it; public/sw.js is git-ignored and the Sep 5 local build's copy holds 393 precache entries. Contrary to §1 ('browserslist now via storybook/webpack/babel devDependencies only'), `@serwist/next ^9.5.12` and `serwist ^9.5.12` sit in package.json dependencies, so serwist is a runtime path. Staleness handling reads sound; the unmeasured cost is 393 asset downloads per returning client per deploy on the Fast Data Transfer meter, which is capped at 100 GB on the Hobby target the owner is aiming at, and the 'deploy count is a lever' section counts only ISR.
  Method: `du -sh .next/static` from a local build to size the manifest; read Vercel Usage -> Fast Data Transfer on the day after the next deploy versus the day before; consider dropping precacheEntries (NetworkOnly already governs freshness) so a deploy costs nothing to returning clients.
- **[low] 'All 7 security headers present' is too vague: the CSP allows 'unsafe-inline' and 'unsafe-eval' in script-src, so it does not mitigate XSS, and style-src still whitelists fonts.googleapis.com although fonts are self-hosted.**
  Why: MEASURED: next.config.ts:257 `script-src 'self' 'unsafe-inline' 'unsafe-eval' https://va.vercel-scripts.com https://vercel.live https://browser.sentry-cdn.com https://*.senja.io https://challenges.cloudflare.com https://analytics.ahrefs.com`; :258 `style-src 'self' 'unsafe-inline' https://fonts.googleapis.com`; :265 frame-ancestors 'none'. Present is not the same as effective; the header row in the report reads as a pass on XSS posture. Not a freeze-window fix (a nonce CSP is a real project), but the checked_ok should say what the header actually permits.
  Method: Record the exact directive in the report; after the freeze, try Next's nonce CSP on a preview deployment and count console violations on / and the app shell.
- **[low] could_not_check rows that were answerable read-only and in two cases contradict a finding already in the report: (a) 'whether the quarterly workflow rebuilds perm_entities and daily_decisions', (b) docs/API.md function-by-function.**
  Why: (a) MEASURED: perm-disclosure-ingest.yml's 'Load the public corpus into Turso' step runs turso_migrate_public.py, one of only two scripts that write perm_entities (the other, rebuild_entities.py, is in no workflow), which S1 already stated; daily_decisions is written by ingest_case_status_direct.py (observed half) and backfill_permtrack.py only (dol-disclosure half, no workflow). So the November 'entities' alarm is a missing stamp on a living table (M8 right) and the dol-disclosure half is genuinely orphaned (S6 right); the row should be closed, not left open. (b) MEASURED with a regex pass: 9 file sections, 67 documented names; 4 documented functions exist nowhere (byDateRange, forDocument, listMine, sendDeadlineReminderEmail) plus 8 heading false positives; 113 of 160 public query/mutation/action exports are undocumented; `auditLogs.ts` is documented but absent from convex/.
  Method: Move both to checked/measured; for API.md either regenerate from `export const` scanning or add a 'partial, last regenerated <date>' banner.
- **[low] Two internal contradictions the synthesis carries without reconciling: the /perm-case-status loading skeleton, and the count of never-refreshed LCA rows.**
  Why: LIVE checked_ok: 'animate-pulse on /perm-case-status is the streamed loading skeleton (aria-hidden, motion-reduce:animate-none), a loading state'; FE-8 lists the same element as a defect shipped in B1. D7: '79,224 LCA rows unrefreshed since Sep 2-3'; §1 and H2: 'the same newest ~100k LCA rows'. Two readers, two numbers (one measured from stamps, one arithmetic from 309,557 minus ~203,700) and two verdicts, both in the report.
  Method: Pick one verdict on the skeleton (the owner's stated rule is no pulses; say so) and cite D7's measured 79,224 with its stamp range instead of the derived ~100k.
- **[low] Fabricated-freshness edges outside H3: a NULL check date renders as 'Today' on the PWD/LCA result, and entity pages print a 'Live case tracker, as of <date>' that can be 30 days old by design.**
  Why: MEASURED: src/components/tools/PwdStatusResult.tsx:278 and LcaStatusResult.tsx:198 render `day(row.lastCheckedAt) ?? "Today"`, the same class as H3 in the opposite direction; src/components/entities/LiveQueueBand.tsx:108 renders `Live case tracker, as of ${longDate(asOf)}` on pages with revalidate = 2592000 where only changed slugs (cap 800) are revalidated nightly, so an unchanged live-only employer keeps a stale 'as of' for up to 30 days while the tracker moved daily. Whether any pwd/lca live row actually has NULL last_checked_at is unmeasured (no index; a count walks 96k/309k rows).
  Method: Change the fallback to 'not yet checked' and have the ingest count NULL stamps into pwd_live_summary; reword the band to 'checked daily; this page rendered <date>' or drop the date.
- **[low] H1's request and write figures are upper bounds derived from a counter that the same report says can double-charge, and are presented as measured.**
  Why: '~59k dynamic invocations/day' and '~2.2M Turso writes/month' are read off perm_docs.pwd_discovery_budget_2026-09-04 = 74,220 (measured), but T6 says the exec() deadline retry can charge the counter twice per request; the true request count is <= 74,220 and unknown until the Firewall tab is read.
  Method: Label both as 'counter increments, an upper bound'; confirm from Firewall -> Traffic request counts for /perm-case-status on Sep 4.
- **[low] The proposed LCA daily 1/7 slice (H2) has no growth horizon; the table it slices grows 50-75k rows a month.**
  Why: §3 measures lca_case_status at +45k/82k/75k per month (Mar-May); the slice sized at ~44k rows/~25 min today is ~65k rows/~37 min by March 2027 and the weekly last_checked_at stamp volume grows with it, so the 'Sunday writes fall to ~50k' verification target drifts upward every month with nothing saying when to re-size.
  Method: State the slice's row and minute growth per month and set a re-size trigger (e.g. slice > 60 min) in the same section.
- **[low] Owner-goal omission: no GSC reindex list, although B1 changes visible copy on five indexed pages.**
  Why: The owner's standing preference is that turns end with the GSC reindex list submitted top-down against a measured ~11/day quota; B1 edits /perm-case-status, /privacy, /for-attorneys, /perm-rfi-audit and content/guides/perm-case-number-format.mdx and the plan says nothing about re-inspection order or the quota.
  Method: Append a ranked reindex list to B1's verify step (case-status page first, guide second) and note the quota.